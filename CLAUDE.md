# CLAUDE.md — 에듀 세이프 해커톤 빌드

**이 프로젝트는 도전형 해커톤을 위한 것임** (팀 「우매한 봉우리」, 트랙 2 심사 앱 코어).

이 프로젝트는 `docs/`의 기획 문서 3종을 따른다:

- 설계 결정·양보 불가 원칙: `docs/해커톤-design-plan.md` — **논쟁이 생기면 이 문서가 이긴다**
- 작업 순서·완료 기준: `docs/해커톤-implementation-plan.md` — T번호 순서로 진행, 완료 기준은 테스트
- 루브릭 전문·카테고리 매핑·화면 설계: `docs/해커톤-기획서.md`

## 규칙

- 태스크 1개 = 커밋 1개 이상, 커밋 메시지에 T번호 (예: `T2: 루브릭 데이터 + 무결성 테스트`)
- 설계 변경은 design-plan 수정 커밋이 먼저, 코드가 나중
- `npm test` 통과 없이 태스크 완료 없음
- 기존 프로토타입 코드베이스는 열지 않는다
- 푸시는 사람이 한다 — Claude는 로컬 커밋까지만

## 명령어

```bash
npm run dev      # localhost:5174
npm test         # vitest
```
