# 에듀세이프(EduSafe) 자가점검 스킬 — 설계

> 교사가 바이브코딩으로 만든 교육용 앱을 **자기 컴퓨터에서, 자기 Claude Code/Codex 구독으로** 배포 전에 점검하는 스킬의 설계 문서다.
> 항목의 법령 근거는 공식 원문(개인정보 보호법·시행령, 안전성 확보조치 기준 고시 제2026-9호, 교육부 훈령 제476호·예규 제82호·「학습지원 소프트웨어 선정 기준 및 가이드라인」, 개인정보보호위원회 안내서, KISA 가이드)을 직접 대조해 조항·쪽수 단위로 적었다. 서지는 §15.
> 2026-08-26 팀 회의의 **투트랙** 결정 중 트랙 1(교사 자가점검 스킬)을 다룬다. 트랙 2(교육청·심사자용 인증 앱)는 별도 담당(Anawim Lee).

## 0. 이 문서의 규약

이 절은 **문서와 구현이 어긋나지 않게 하는 장치**를 정의한다. 나머지 절보다 먼저 읽는다.

### 0.1 요구사항 ID

구현자가 반드시 지켜야 하는 문장(**규범 문장**)에는 `[REQ-<절>.<번호>]` 를 붙인다. 규범 문장인지의 판정 기준은 하나다 — **지켜지지 않으면 그것은 결함인가?** 배경 설명·근거·예시에는 붙이지 않는다.

`[REQ-0.1]` 구현 계획의 각 Task 는 자신이 구현하는 REQ 를 `**Implements:**` 로 선언하고, 그 REQ 의 원문을 인용 블록으로 **전사**한다. 요약하거나 의역하지 않는다.

`[REQ-0.2]` 이 문서의 모든 REQ 는 구현 계획의 어떤 Task 에 최소 한 번 할당되어야 한다. 미할당 REQ 가 하나라도 있으면 구현 계획은 미완성이다.

`[REQ-0.3]` 구현 계획이 참조하는 REQ 는 이 문서에 실재해야 한다.

REQ-0.1~0.3 은 사람이 지키는 규율이 아니라 **테스트가 강제한다**(§12.3).

### 0.2 데이터 정본

`[REQ-0.4]` §6(항목 37개·하위 점검 134개)과 §9·§9.1(스캔 규칙 48개)은 **구현 데이터의 정본**이다. `items.json`·`scan-rules.mjs` 는 이 절들을 전사한 결과이며, 둘이 어긋나면 문서가 옳고 구현이 틀린 것이다.

`[REQ-0.5]` 문서와 구현의 일치는 테스트가 id 단위로 **양방향** 대조한다(§12.3). 문서에만 있는 id 와 구현에만 있는 id 둘 다 실패다.

### 0.3 자기완결성

`[REQ-0.6]` 이 문서와 구현 계획 문서 두 개만으로 구현이 끝나야 한다. **문서 밖의 정보**(다른 저장소의 파일·이전 구현·웹 문서·팀 내부 자료)를 찾아봐야 하는 상황이 생기면 그것은 문서의 결함이다 — 진행하지 말고 문서를 먼저 고친다. 일반 프로그래밍 지식과 개발 도구 설치(`npm install`)는 여기서 말하는 "문서 밖 정보"가 아니다. 실행 검증(§12.5)은 성격상 실행 환경과 검사 대상 앱이 필요하며, 이는 문서의 결함이 아니다.

`[REQ-0.7]` 두 문서는 드라이브 문자로 시작하는 절대경로(`C:\…`)나 다른 저장소의 경로를 참조하지 않는다. 저장소 루트는 `$REPO` 로 표기한다. 스킬이 설치되는 사용자 폴더(`~/.claude/skills/edusafe/`)처럼 실행 환경을 가리키는 일반 경로는 예외다.

### 0.4 검증되는 표면만 만든다

`[REQ-0.8]` 보고서에 렌더링되는 모든 구조는 스키마가 형태를 검증한다. **검증할 수 없으면 렌더링하지 않는다.** 이 원칙의 구체적 강제 방법은 §8.3 이 정한다.

이 원칙이 있는 이유: 검증되는 표면(합계·상태값)은 오류가 즉시 드러나지만, 검증되지 않는 표면(중첩 배열·객체)은 **조용히 비어도 아무도 모른다**. 절차서에 "빠짐없이 채우라"고 적는 것은 이 실패 유형을 막지 못한다 — 검사자는 채웠다고 믿기 때문이다. 막는 방법은 하나뿐이다: 채우지 않으면 렌더가 거부하는 구조적 슬롯.

## 1. 배경과 목적

교사가 바이브코딩으로 만든 앱은 보안·개인정보 지식 없이 만들어지는 경우가 대부분이다. 심사자가 GitHub API로 소스를 긁어 AI가 한 번에 판정하는 방식은 ① 200파일/8MB/150k자 한도에서 잘리고 ② 빌드·실행·git 히스토리를 볼 수 없으며 ③ 심사자 API 키가 필요하고 ④ 교사 60~70%가 자기 repo 공개를 꺼린다. 첫 심사(Community_builders, 2026-08-24)에서 24항목 중 판단불가 16건이 나온 실측이 이 한계의 결과다.

팀 회의는 **투트랙**으로 결정했다:

- **트랙 1 — 에듀세이프 스킬 (이 문서)**: 교사가 **자기 컴퓨터에서, 자기 구독으로** 배포 전에 점검. 보안 + 개인정보 필수 요소에 집중, 가볍게. 산출물은 교사 본인용이며 인증 증거가 아니다.
- **트랙 2 — 인증 앱**: 교육청·심사자가 repo/zip을 받아 폐쇄 환경에서 재검사. 카테고리·성취기준·AI 윤리·교육 적절성은 여기서.

스킬의 목적: **교사가 무엇을 고쳐야 하는지 근거와 함께 알게 한다.** 인증·점수·통과 판정은 하지 않는다.

## 2. 확정 결정 요약

| # | 쟁점 | 결정 |
|---|---|---|
| 1 | 검사 깊이 | **스캔 범위 내 소스 전수(스캐너) + git 히스토리 + 빌드 산출물(가능 시)**. 빌드 생략은 실패가 아니지만, 빌드가 필요한 하위 점검은 판단불가로 남긴다 |
| 2 | 고쳐주기 | **지적 + 근거 + 수정 방법 안내**. 스킬은 코드를 수정하지 않는다. 수정은 보고서 완료 후 교사의 별도 요청·별도 승인으로 시작하는 다른 작업이다 |
| 3 | 실행 환경 | **Claude Code + Codex 둘 다**. 도구 중립 `SKILL.md` |
| 4 | 모드 | **자가점검 단일 모드**. 심사자 모드는 후속 |
| 5 | 판단불가 처리 | **증거 제출형(규칙 붙여넣기) + 교사 확인형(라벨 구분)** 혼합. 교사 답변은 판단불가만 대체하며 검증된 미충족을 해제하지 못한다 |
| 6 | 종합 판정 | **행동 중심 3단계**(반드시 수정/권장/참고) + 판단불가·교사확인 건수. 점수 없음 |
| 7 | 스택 | **웹앱 정식 지원**(HTML/JS · React/Vite · Next.js + Firebase/Supabase) + 그 외는 일반 규칙만 적용하고 미지원 항목은 판단불가(사유 `unsupported-stack`) |
| 8 | 배포·설치 | **안내 웹페이지에서 zip 다운로드 → 사용자 전역 스킬 폴더에 수동 배치**, 도구·OS별 안내 |
| 9 | 항목 근거 | **공식 원문 직접 인용**(고시 조항·안내서 쪽수). 서지는 §15 |
| 10 | 항목 ID | 근거 문서가 명명한 v1.2 ID 계승 + 신규는 같은 규칙으로 스킬이 먼저 명명 → 트랙 2와 공유 |
| 11 | 결정적 스캔 | **의존성 0 Node 스크립트**(§9·§9.1 규칙 48개) 동봉. Node 없으면 에이전트 직접 스캔으로 강등(부재 증명 항목은 pass 불가) |
| 12 | 저장소 | **독립 git 저장소**. 이 문서에서 저장소 루트는 `$REPO` |
| 13 | 산출물 | **HTML 1순위**(스크립트 렌더링, JS 없음) + JSON(정본 `edusafe-report.json`) + MD(대체본) + `scan.json`, `history/` 최대 5개 보관 |
| 14 | 변조 | 방지 불가를 전제. 자가보고 지문·재현 재료는 우발적 수정 탐지용이며, 인증은 트랙 2가 공식 배포본으로 재실행 |
| 15 | 학교 절차 | 보고서에 **교육부 「학습지원 소프트웨어 선정기준」[서식 1] 필수기준 대조표**를 함께 출력 — 학운위 심의 준비 자료로 바로 쓰이게 |

## 3. 범위와 비범위

**범위 (v0.1)**: 8개 카테고리 37개 항목 자가점검 · 프로젝트 프로필 산출 · 동봉 스캐너 · git 히스토리 키 스캔 · 선택적 빌드 산출물 스캔 · DB 도달 경로·목적지 인벤토리 · 확인 세션 · HTML/JSON/MD 보고서 · 교육부 서식1 대조표 · **보고서 필드 계약(§8.3)과 그 계약을 강제하는 검증기** · zip 배포 · 픽스처(골든 판정표) 기반 테스트 · **문서 린터**(§12.3) · 실제 앱 2건 현실성 확인.

**비범위**: 심사자 모드 · 능동 프로브(운영 DB에 쓰기 시도) · CI/배지 파이프라인 · 카테고리 분류·성취기준·AI 윤리·교육 적절성·사용성 항목 · Apps Script/Python 정식 지원 · 자동 코드 수정 · 안내 웹페이지 · 서명·난독화 등 변조 방지 · git 서브모듈 · 빌드 시 네트워크 차단·환경변수 격리(§10).

**법령 원문**: 항목의 `basis` 는 조항·쪽수 단위로 특정하되 원문 전문은 이 문서에 싣지 않는다. 이 문서의 자기완결성은 **구현**에 대한 것이지 법령 해석의 재검증에 대한 것이 아니다.

**"자동 검증 비범위이나 확인형 안내는 포함"**: 접속기록 보관(고시 제8조)·내부 관리계획(제4조)·악성프로그램(제9조)·물리적 안전조치(제10조)는 코드에서 검증하지 않으며 보고서 각주로만 안내한다. 반면 H-2fa·H-retention·H-delete·H-breach-ready·H-school-approval은 코드 밖 사안이지만 **확인형 항목으로 포함**한다(교사가 모르면 사고로 이어지는 축이라서).

## 4. 구조

```
$REPO/                               (git 저장소)
 ├─ edusafe/                         ← 배포 단위 = zip. 의존성 0
 │   ├─ SKILL.md                     ← 절차서 (frontmatter: name·description만)
 │   ├─ rules/
 │   │   ├─ version.json             ← 버전 정본 (README·보고서·zip 이름이 참조)
 │   │   ├─ items.json               ← 항목 정본 (§6 전사)
 │   │   ├─ moe-checklist.json       ← 교육부 [서식 1] 필수기준 ↔ 우리 항목 매핑
 │   │   ├─ session.json             ← 확인 세션 질문 정본 (§7.5 전사)
 │   │   ├─ scan-rules.mjs           ← 결정적 패턴 (§9 전사 + 정규식)
 │   │   └─ report.contract.json     ← 보고서 필드 계약 (§8.3 전사)
 │   ├─ scripts/
 │   │   ├─ scan.mjs                 ← 폴더 스캔 → scan.json (범위·파일 해시 기록, 마스킹)
 │   │   └─ render.mjs               ← edusafe-report.json → HTML + MD, 계약 검증, 지문 계산, history 이동
 │   ├─ templates/report.html        ← HTML 골격 (CSS 인라인, JS 없음, CSP meta)
 │   └─ README.md                    ← 버전·설치(도구·OS별)·사용·신뢰 경계 안내
 ├─ tests/                           ← vitest: 단위·회귀 + 문서 린터(§12.3)
 ├─ fixtures/                        ← 취약점을 고의로 심은 가짜 앱 + 골든 판정표
 ├─ docs/superpowers/{specs,plans}/  ← 이 문서와 구현 계획. 테스트가 읽는다
 ├─ scripts/build-zip.mjs            ← 배포 zip + sha256 + manifest
 └─ package.json                     ← 테스트·zip 빌드 스크립트만 (스킬 폴더는 사용 안 함)
```

원칙:

`[REQ-4.1]` `SKILL.md` 는 절차만 담고 데이터는 `rules/` 에 둔다. 항목을 개정할 때 절차서를 건드리지 않는다.

`[REQ-4.2]` `edusafe/` 는 외부 의존성이 0이다. `scan.mjs`·`render.mjs` 는 Node 내장 모듈만 사용하며 교사가 `npm install` 없이 실행한다.

`[REQ-4.3]` AI 는 `edusafe-report.json` 만 작성하고 HTML·MD 는 `render.mjs` 가 그 JSON 에서 렌더한다. 두 형식이 어긋날 수 없다.

`[REQ-4.4]` `docs/superpowers/` 의 두 문서는 배포 zip 에 들어가지 않지만 저장소에는 남는다 — 테스트가 읽어야 하기 때문이다(§12.3).

## 5. 실행 흐름 (`/edusafe` · Codex는 `$edusafe`)

### 5.1 단계표

이 표의 **"사람이 없을 때"** 열이 이 절의 핵심이다. 스킬은 비대화형(`claude -p` 등)으로도 실행되며, 그때 사람의 응답을 기다리면 **아무 산출물도 나오지 않는다.**

| 단계 | 하는 일 | 대기·승인 | 사람이 없을 때 | coverage 기록 |
|---|---|---|---|---|
| 0 준비 | 프로젝트 루트·스택·git 유무·Node 유무 감지. 남은 `.staging-*` 정리 | 없음 | 해당 없음 — 감지 결과 한 줄 보고 후 바로 1단계 | `runtime` |
| 1 스캔 | `scripts/scan.mjs` 실행 → `scan.json` | 없음 | 해당 없음 | `scanner` |
| 2 히스토리 | git 로컬 ref 전체에서 키·`.env`·데이터 파일 스캔 | 없음 | 해당 없음 | `history` |
| 3 빌드(선택) | install·build 명령을 보여주고 각각 승인 후 실행 | **있음** | **묻지 않고 건너뛴다.** 단 기존 빌드 산출물이 이미 있으면 빌드 없이 읽기 전용으로 스캔한다 | `build` |
| 4a 프로필 | data_inventory·actors·entry·controller·db_paths·destinations 작성 | 없음 | 해당 없음 | `code` |
| 4b 판정 | 항목 37개를 카테고리 순으로 판정 | 없음 | 해당 없음 | `code` |
| 5 판정 마무리 | needs_human 확정, 확인 후보 목록화 | 없음(질문 금지) | 해당 없음 — 질문하지 않고 목록만 만든다 | — |
| 6 보고서 | `edusafe-report.json` 작성 → `render.mjs` | 없음 | 해당 없음 — **여기서 보고서가 완성된다** | — |
| 7 선택적 갱신 | 확인 후보를 질문으로 제시하고 답이 오면 갱신 | **있음** | **6의 보고서가 최종본이다** | `evidence`·`teacher` |

`[REQ-5.1]` 0~6단계는 사람의 응답 없이 끝까지 진행한다. 질문을 던지고 기다리는 단계는 3단계(빌드 승인)와 7단계(확인 세션)뿐이다.

`[REQ-5.2]` 위 표에서 "대기·승인"이 **있음**인 모든 단계는 "사람이 없을 때"의 동작이 정의되어 있어야 한다. 동작이 정의되지 않은 대기 지점을 두지 않는다.

`[REQ-5.3]` 이 실행이 대화형인지 스킬은 스스로 판단할 수 없다. 따라서 5단계는 질문을 던지지 않고 확인 후보를 목록으로만 정리한다.

`[REQ-5.4]` 6단계까지 한 번의 실행에서 반드시 끝난다. 확인 후보가 있어도 기다리지 않고 보고서를 완성한다.

`[REQ-5.5]` 스킬은 프로젝트 파일을 수정하지 않는다. `edusafe-report/` 를 만드는 것만 한다.

### 5.2 단계별 상세

**0단계 — 준비**
`package.json`·`firebase.json`·`firestore.rules`·`supabase/` 등으로 스택을, `.git` 으로 git 유무를, `node --version` 으로 Node 유무를 감지한다.

`[REQ-5.6]` 직전 실행이 중간에 끝난 흔적(`edusafe-report/.staging-*`)이 있으면 지우고 새로 만든다.

**1단계 — 결정적 스캔**

`[REQ-5.7]` 스캔 범위: 포함 확장자 목록에 해당하는 파일. 제외 = `node_modules`·`dist`·`.next`·`build`·`out`·`.git`·`edusafe-report/`·2MB 초과·바이너리·symlink.

`[REQ-5.8]` `scan.json` 에는 검사한 파일별 `{path, sha256}`, 건너뛴 파일과 사유, 규칙별 hit 을 기록한다. 시크릿 인용은 **저장 전에** 마스킹한다.

`[REQ-5.9]` Node 가 없으면 에이전트가 직접 스캔하고 `coverage.scanner.status = "agent-fallback"` 으로 기록한다. 이때 부재 증명 항목은 pass 할 수 없다.

**2단계 — git 히스토리**

`[REQ-5.10]` git 이 있으면 모든 로컬 ref(`--all`)의 히스토리에서 키 패턴·`.env`·데이터 파일을 스캔한다. 한도는 120초 또는 diff 200MB 이며, 초과하면 중단하고 검사한 ref·커밋 수를 기록한다. shallow clone 이면 그 사실을 표시한다.

`[REQ-5.11]` git 이 없으면 `coverage.history = { status: "skipped", reason: "no-git" }` 을 기록한다.

**3단계 — 빌드 산출물 (선택)**

`[REQ-5.12]` 빌드 스크립트가 있으면 실행할 정확한 명령(install / build)과 작업 디렉터리를 먼저 보여주고 **각각 승인**받는다. 승인 화면에는 다음 고지를 함께 표시한다: "빌드 스크립트와 의존성은 임의 코드를 실행할 수 있고 이 컴퓨터의 환경변수·토큰에 접근할 수 있습니다".

`[REQ-5.13]` 빌드 시간 제한은 10분이고, 스캔할 산출물 경로는 허용목록(`dist`·`.next`·`build`·`out`)으로 제한한다.

`[REQ-5.14]` 사람의 응답을 받을 수 없으면 빌드를 실행하지 않고 건너뛴다: `coverage.build = { status: "skipped", reason: "non-interactive" }`.

`[REQ-5.15]` 빌드하지 않더라도 허용목록 경로에 **기존 빌드 산출물이 이미 있으면** 빌드를 실행하지 않고 읽기 전용으로 스캔한다: `coverage.build = { status: "ran", reason: "prebuilt-artifact" }`. 이 경우 산출물이 현재 소스와 일치한다는 보장이 없다는 사실을 보고서에 함께 적는다.

`[REQ-5.16]` 빌드 실패·거부·건너뜀 시 빌드가 필요한 하위 점검은 `needs_human` 으로 남는다.

**4a단계 — 프로필**

`[REQ-5.17]` 다음을 작성한다: `data_inventory`(수집·저장 필드) · `actors`(익명·학생·교사·관리자) · `entry`(로그인·입장 방식) · `controller`(school / teacher_personal / unknown) · `student_facing` · `trusted_outcomes`.

`[REQ-5.18]` **DB 도달 경로 인벤토리**(`db_paths`)와 **목적지 인벤토리**(`destinations`)를 작성한다. 이 둘은 카테고리 2·4 판정의 입력이다.

**4b단계 — 판정**

`[REQ-5.19]` `items.json` 37개를 카테고리 순으로 판정한다. `applicability` 조건에 걸리면 `na`(사유 필수), 아니면 판정한다.

`[REQ-5.20]` 부재 증명 항목은 스캐너의 `negative_scan` 근거로 판정하고, 나머지는 스캔 hit·인벤토리·입력 폼·저장 호출을 진입점으로 코드를 추적해 판정한다. 근거를 대지 못하면 `needs_human` 후보다.

**5단계 — 판정 마무리**

`[REQ-5.21]` 4b에서 남은 `needs_human` 후보를 확정하고, `session.kind` 가 있는 항목만 확인 후보 목록(증거 제출형 / 교사 확인형)으로 정리해 둔다. 질문을 던지지 않고 기다리지 않는다.

**6단계 — 보고서**

`[REQ-5.22]` `edusafe-report.json` 을 작성하고(`session` 은 아직 빈 배열) `render.mjs` 를 실행한다. 렌더는 계약 검증 → staging 렌더 → 4파일 검증 → 직전 결과 history 이동 → 교체 순으로 진행한다(§8.2).

**7단계 — 선택적 갱신 안내**

`[REQ-5.23]` 완료 메시지는 종합 판정 5줄 + HTML 절대 경로 + "수정을 원하시면 별도로 '항목 ID 고쳐줘'라고 요청하세요 (보고서는 수정 전 상태)" 를 담는다.

`[REQ-5.24]` 5단계에서 모은 확인 후보를 질문 목록으로 제시하되 "답하지 않으셔도 보고서는 이미 완성돼 있습니다"를 함께 알리고 건너뛰기 선택지를 준다. 답이 오면 해당 항목만 갱신하고 새 staging 으로 6단계를 다시 수행한다. 답이 없으면 6단계의 보고서가 그대로 최종본이다.

`[REQ-5.25]` SKILL.md 는 스크립트를 자신의 부모 폴더 기준 상대 경로(`scripts/scan.mjs`)로 지시한다. 경로를 얻을 수 없으면 전역 경로를 임의 탐색하지 않고 사용자에게 설치 경로를 1회 확인받으며, 그래도 없으면 `coverage.runtime = "degraded"` 로 강등 실행한다.

## 6. 항목 — 8 카테고리 37개

이 절은 **항목 데이터의 정본**이다(REQ-0.4). `items.json` 은 이 절을 전사한 결과이며, 테스트가 id 단위로 양방향 대조한다(§12.3).

집계: **상(high) 14 · 중(medium) 18 · 하(low) 5 = 37**. 하위 점검 **134개**.

**중요도(`base_severity`)**: `high` = 개인정보 유출·위조·법 위반이 직접 발생 / `medium` = 구조적 취약이나 즉시 유출은 아님 / `low` = 안내·운영 관행. 항목당 한 값이며, 상황에 따라 달라지는 경우는 `effective_severity` 결정표(§7.6)로 기록한다.

**판정 방식(`methods`)**: `scanner`(결정적) · `code`(AI 코드 추적) · `evidence`(교사가 규칙·설정 붙여넣기) · `teacher`(교사 답변, 라벨 구분).

**`required_coverage`**: 하위 점검마다 `scanner`·`history`·`build`·`code`·`evidence`·`teacher` 중 필요한 것을 적는다. 해당 coverage 가 확보되지 않으면 그 하위 점검은 `needs_human` 이다.

**`stacks`**: `all` 또는 `html`·`vite-react`·`nextjs`·`firebase`·`supabase` 중 일부. 정식 지원 스택이 아니면 스택 특화 하위 점검은 `needs_human`(사유 `unsupported-stack`)이다.

`[REQ-6.1]` 각 항목은 **독립된 보안 축**을 묻는다. 같은 근거를 여러 항목이 인용할 수 있다.

`[REQ-6.2]` **부재 증명 항목**("없다"를 확인해야 하는 항목)은 한 줄 인용으로 pass 할 수 없다. pass 근거는 `negative_scan` 유형 — 스캐너가 어떤 규칙으로 어느 범위(파일 목록·제외)를 검사했는지의 기록이다. 스캐너 미실행(`agent-fallback`)이거나 범위가 미달이면 pass 가 아니라 `needs_human`(사유 `coverage-insufficient`)이다.

`[REQ-6.3]` 정식 지원 스택(HTML/JS · React/Vite · Next.js + Firebase/Supabase)에서는 37개 항목이 전부 활성이다. 그 외 스택에서는 스택 무관 항목만 판정하고 스택 특화 항목은 `needs_human`(사유 `unsupported-stack`)으로 남긴다.

**적용 범위 각주** (보고서에 함께 출력):

- 고시 **제3조**: "보유 수·유형·정보주체에게 미치는 영향 등을 고려하여 **스스로의 환경에 맞는** 조치" — 교사 앱에 대기업 수준을 요구하지 않는다.
- 고시 **제4조 단서**: 1만명 미만을 처리하는 소상공인·개인·단체는 **내부 관리계획 수립·시행 생략 가능**. 단 제5~13조(접근권한·접근통제·암호화·파기 등)는 적용된다.
- 접속기록 보관(제8조)·악성프로그램(제9조)·물리적 안전조치(제10조)는 코드 밖 운영 영역이라 이 스킬의 검사 대상이 아니며, 보고서 각주로만 안내한다.


### 6.1 카테고리 1. 수집 — 무엇을 모으나

#### R-rrn — 주민등록번호를 수집·보관·처리하지 않는다

| 속성 | 값 |
|---|---|
| 카테고리 | 1 |
| 중요도 | high |
| 판정 방식 | scanner, code |
| 해당없음 조건 | 해당없음 없음 — 부재 증명 항목 |
| 부재 증명 항목 | 예 |
| 근거 | 개인정보 보호법 제24조의2 |

| 하위 점검 id | 내용 | required_coverage | stacks |
|---|---|---|---|
| `rrn-field` | 주민번호 입력 필드 | scanner | all |
| `rrn-pattern` | 13자리 주민번호 형식 데이터 | scanner | all |
| `rrn-var` | jumin/rrn 등 변수명 | scanner | all |

**왜 위험한가** — 주민등록번호는 법에서 정한 경우가 아니면 수집 자체가 금지되어 있고, 한 번 유출되면 바꿀 수 없습니다.

**수정 방법** — 학번·닉네임·임의 코드로 대체하세요. 이미 저장된 값이 있다면 즉시 삭제하고, 삭제 후 복구되지 않도록 처리하세요.

#### R-under14 — 만 14세 미만 아동의 개인정보를 처리한다면 연령 확인과 법정대리인 동의 절차가 있다

| 속성 | 값 |
|---|---|
| 카테고리 | 1 |
| 중요도 | high |
| 판정 방식 | code, teacher |
| 해당없음 조건 | 개인정보를 수집하지 않는 경우 |
| 부재 증명 항목 | 아니오 |
| 근거 | 법 제22조의2 + 시행령 제17조의2(동의 확인 7방법) + 아동·청소년 개인정보 보호 안내서(2024.12) 1-1·p.31~32·4-2 |

| 하위 점검 id | 내용 | required_coverage | stacks |
|---|---|---|---|
| `age-check` | 연령 확인 절차 — 생년월일 직접 입력 또는 '만 14세 이상' 체크 | code | all |
| `consent-flow` | 법정대리인 동의 안내 화면·절차 | code | all |
| `consent-attested` | 시행령 제17조의2의 7가지 방법 중 하나(서면·문자·이메일·전화 등, 종이 서면도 적법)로 확인했는가 | teacher | all |
| `consent-5day-purge` | 동의 거부·미확인 상태로 5일 경과 시 파기 | code, teacher | all |

**왜 위험한가** — 법정대리인 동의 없이 만 14세 미만 아동의 개인정보를 수집하면 5년 이하 징역 또는 5천만원 이하 벌금·과징금 대상입니다.

**수정 방법** — 가입 화면에서 생년월일을 받거나 '만 14세 이상' 체크를 두고, 미만이면 보호자 동의 절차로 보내세요. 종이 동의서를 받아 관리자가 기록하는 방식도 적법합니다.

#### S-sensitive — 감정·건강·상담·위치·영상·음성 기록은 익명·가명으로 다루거나 기기 밖으로 내보내지 않는다

| 속성 | 값 |
|---|---|
| 카테고리 | 1 |
| 중요도 | high |
| 판정 방식 | code |
| 해당없음 조건 | 민감정보 없음 |
| 부재 증명 항목 | 아니오 |
| 근거 | 법 제23조 + 안전성 확보조치 기준(고시 제2026-9호) 제7조 |

| 하위 점검 id | 내용 | required_coverage | stacks |
|---|---|---|---|
| `storage-identifier-type` | 저장 구조·식별자 종류 | code | all |
| `location-camera-mic-data` | 위치/카메라/마이크 데이터의 저장·전송 | code | all |

**왜 위험한가** — 감정·건강·상담 등 민감한 기록은 유출되면 학생에게 더 큰 피해를 줄 수 있어 일반 개인정보보다 더 조심히 다뤄야 합니다.

**수정 방법** — 민감한 기록은 실명 대신 임의의 코드로 저장하고, 위치·카메라·마이크로 얻은 데이터는 꼭 필요한 경우가 아니면 기기 밖(서버)으로 보내지 마세요.

#### S-minimal — 꼭 필요한 최소한의 개인정보만 수집한다

| 속성 | 값 |
|---|---|
| 카테고리 | 1 |
| 중요도 | medium |
| 판정 방식 | code |
| 해당없음 조건 | 개인정보 미수집 |
| 부재 증명 항목 | 아니오 |
| 근거 | 법 제3조·제16조 + 아동 안내서 1-2·1-3·1-4 |

| 하위 점검 id | 내용 | required_coverage | stacks |
|---|---|---|---|
| `field-vs-necessity` | 입력·저장 필드 vs 필요성 | code | all |
| `nickname-alternative` | 닉네임·임의코드 대체 가능 여부 | code | all |
| `location-tracking-default-off` | 위치추적 옵션 비활성화 기본값 | code | all |
| `permission-necessity-granular` | 기기 권한(위치·카메라·마이크)의 필요성·사유 안내·세분화 제시('허용/거부'만 지양) | code | all |
| `incentive-for-pii` | 현금·아이템 대가로 개인정보 입력 유도 없음 | code | all |

**왜 위험한가** — 꼭 필요하지 않은 개인정보까지 모으면 그만큼 유출됐을 때 피해 범위가 커지고, 나중에 관리해야 할 정보도 늘어납니다.

**수정 방법** — 서비스에 실제로 필요한 항목만 입력받고, 이름 대신 닉네임을 쓰게 하고, 위치·카메라 등 기기 권한은 꼭 필요한 기능에만 개별적으로 요청하세요.

### 6.2 카테고리 2. 접근·권한 — 누가 무엇을 할 수 있나

이 카테고리의 판정은 4a에서 만든 **DB 도달 경로 인벤토리(`db_paths`)** 를 입력으로 쓴다. 경로마다 인증·소유권·역할·검증·호출제한 다섯 통제를 확인한다.

#### R-db-locked — 비로그인 상태로 쓸 수 없다 (누구나×쓰기)

| 속성 | 값 |
|---|---|
| 카테고리 | 2 |
| 중요도 | high |
| 판정 방식 | scanner, code, evidence |
| 해당없음 조건 | 외부 DB 미사용 |
| 부재 증명 항목 | 아니오 |
| 근거 | 고시 제6조③("인터넷 홈페이지·P2P·공유설정 등을 통하여 권한이 없는 자에게 공개되거나 유출되지 않도록") + API 유출예방(2026.7) "인증·권한 없는 요청 기본 차단" |

| 하위 점검 id | 내용 | required_coverage | stacks |
|---|---|---|---|
| `firestore-open-write` | Firestore/RTDB `allow write: if true`·`.write: true` | scanner | firebase |
| `supabase-rls-missing` | Supabase `create table` 후 RLS 미활성 | scanner | supabase |
| `supabase-write-policy-open` | 쓰기 정책 `using(true)`/`with check(true)` | scanner | supabase |
| `deny-by-default` | 인증·권한 없는 요청은 기본 차단(deny by default) | code | all |
| `rules-file-missing-evidence` | 규칙 파일 없으면 증거 요청 + "규칙 파일을 저장소에 포함" 권고 | evidence | all |

**왜 위험한가** — 로그인하지 않은 사람도 데이터베이스에 쓸 수 있으면, 누구나 학생 정보를 마음대로 추가·변경·삭제할 수 있게 됩니다.

**수정 방법** — 로그인한 사용자만 쓰기가 가능하도록 Firestore·Supabase 등의 보안 규칙을 설정하고, "허용"을 기본값으로 두지 마세요.

#### S-access — 남의 데이터를 볼 수 없다 (타인×읽기)

| 속성 | 값 |
|---|---|
| 카테고리 | 2 |
| 중요도 | high |
| 판정 방식 | code, evidence |
| 해당없음 조건 | 사용자·반·모둠 단위 데이터 없음 |
| 부재 증명 항목 | 아니오 |
| 근거 | API 유출예방(2026.7) "로그인 여부만 확인하고 개인정보 조회 권한을 확인하지 않거나" + 고시 제5조①·제6조③ |

| 하위 점검 id | 내용 | required_coverage | stacks |
|---|---|---|---|
| `auth-only-not-ownership` | 로그인 여부만 확인하고 조회 권한을 확인하지 않는 구조(`request.auth != null`만으로 전체 읽기) | code | all |
| `class-code-guessable` | 반 코드 추측 가능성(4자리 등) | code | all |
| `shared-class-code-access` | 반 전체가 공유하는 코드·계정만으로 개인정보 접근(고시 제6조③ "권한이 없는 자에게 공개" — 제5조④가 아님) | code | all |
| `idor-url-id` | URL id 변경으로 타인 결과 열람(IDOR) | code | all |
| `class-group-data-scope` | 반·모둠 공유 데이터의 범위 | code | all |
| `teacher-data-student-exposure` | 교사 데이터의 학생 노출 | code | all |

**왜 위험한가** — 로그인 여부만 확인하고 "내 데이터인지"는 확인하지 않으면, 로그인한 아무나 다른 학생의 이름·점수·기록을 볼 수 있습니다.

**수정 방법** — 데이터를 읽을 때 요청한 사람이 그 데이터의 주인(또는 같은 반 교사 등 허용된 사람)인지 서버에서 반드시 확인하도록 규칙을 작성하세요.

#### R-impersonate — 남의 데이터를 고칠 수 없다 (타인×쓰기)

| 속성 | 값 |
|---|---|
| 카테고리 | 2 |
| 중요도 | high |
| 판정 방식 | code, evidence |
| 해당없음 조건 | 사용자별 데이터 없음 |
| 부재 증명 항목 | 아니오 |
| 근거 | API 유출예방 "모든 API 요청에 대해 요청한 이용자가 해당 개인정보를 조회하거나 수정할 권한이 있는지 서버에서 확인" + 고시 제5조① |

| 하위 점검 id | 내용 | required_coverage | stacks |
|---|---|---|---|
| `client-doc-id-no-uid-match` | 클라이언트 지정 문서 ID에 uid 매칭 없는 쓰기 | code | all |
| `nickname-only-impersonation` | 닉네임만으로 입장하는 사칭 | code | all |
| `server-side-authz-check` | 모든 요청에 대해 요청자가 그 정보를 조회·수정할 권한이 있는지 서버에서 확인 | code | all |

**왜 위험한가** — 다른 사람 행세를 하거나 남의 데이터를 지정해서 고칠 수 있으면, 학생이 다른 학생의 점수나 기록을 바꿔치기할 수 있습니다.

**수정 방법** — 데이터를 수정할 때도 요청자가 그 데이터의 실제 주인인지 서버가 확인하도록 하고, 닉네임만으로 로그인을 대신하지 마세요.

#### R-score-forge — 내 점수·완료·보상을 위조할 수 없다 (본인×신뢰 필드)

| 속성 | 값 |
|---|---|
| 카테고리 | 2 |
| 중요도 | high |
| 판정 방식 | code |
| 해당없음 조건 | trusted_outcomes 없음 |
| 부재 증명 항목 | 아니오 |
| 근거 | 법 제29조(안전조치 — 학습 기록의 무결성) + 개발보안 가이드(신뢰할 수 없는 입력). 실측 사고 기반 |

| 하위 점검 id | 내용 | required_coverage | stacks |
|---|---|---|---|
| `client-computed-value-write` | 클라이언트 계산 값의 직접 write | code | all |
| `server-side-computation-or-validation` | 서버 계산 또는 규칙 검증 여부 | code | all |

**왜 위험한가** — 점수나 완료 여부를 학생 기기(클라이언트)에서 계산해 그대로 저장하면, 개발자 도구로 값을 바꿔 점수를 조작할 수 있습니다.

**수정 방법** — 점수·완료 같은 중요한 값은 서버(함수·RPC)에서 계산하거나, 최소한 저장 규칙에서 값의 범위·조건을 검증하도록 만드세요.

#### S-upload-exposure — 학생이 올린 파일이 공개 URL로 접근되지 않고, 업로드가 통제된다 (누구나×파일)

| 속성 | 값 |
|---|---|
| 카테고리 | 2 |
| 중요도 | high |
| 판정 방식 | scanner, code, evidence |
| 해당없음 조건 | 파일 업로드 없음 |
| 부재 증명 항목 | 아니오 |
| 근거 | 법 제23조·제29조 + 고시 제6조③ + 아동 안내서 1-2(높은 수준 기본값) |

| 하위 점검 id | 내용 | required_coverage | stacks |
|---|---|---|---|
| `supabase-public-bucket` | Supabase public 버킷 | scanner | supabase |
| `storage-rule-read-true` | Storage 규칙 `read: true` | scanner | firebase |
| `upload-size-format-limit` | 업로드 크기·형식 제한 | code | all |
| `executable-content-blocked` | 실행 가능 콘텐츠(html/svg/js) 차단 | code | all |
| `private-default-scope` | 공개 범위 기본값이 '비공개'인가(전체공개 기본값 금지) | code | all |

**왜 위험한가** — 학생이 올린 사진·파일이 누구나 접근 가능한 주소로 공개되면, 관련 없는 사람도 그 파일을 볼 수 있게 됩니다.

**수정 방법** — 업로드 저장 공간을 기본적으로 비공개로 설정하고, 파일 크기·형식을 제한하며, 실행되는 코드가 담긴 파일은 업로드되지 않도록 막으세요.

#### S-password-storage — 자체 로그인이 있다면 비밀번호를 복호화 불가능한 일방향 해시(솔트 포함)로 저장하고 평문으로 저장·전송·기록하지 않는다

| 속성 | 값 |
|---|---|
| 카테고리 | 2 |
| 중요도 | high |
| 판정 방식 | scanner, code |
| 해당없음 조건 | 자체 비밀번호 인증 없음(구글 로그인 등만) |
| 부재 증명 항목 | 아니오 |
| 근거 | 고시 제7조① "비밀번호를 저장하는 경우에는 복호화되지 아니하도록 일방향 암호화" + KISA JS 시큐어코딩 2절 5·9·14 |

| 하위 점검 id | 내용 | required_coverage | stacks |
|---|---|---|---|
| `plaintext-password-compare` | 평문 비교(`password === `) | scanner | all |
| `plaintext-password-storage` | localStorage·DB 평문 저장 | scanner | all |
| `hash-library-missing` | 해시 라이브러리 부재 | code | all |
| `unsalted-hash` | 솔트 없는 해시(MD5/SHA1 단독) | scanner | all |
| `password-exposed-url-log` | 비밀번호를 URL·로그에 노출 | scanner | all |

**왜 위험한가** — 비밀번호를 그대로(평문) 저장하거나 약한 방식으로만 암호화하면, 저장된 자료가 새어나갈 때 학생들의 비밀번호가 그대로 드러납니다.

**수정 방법** — bcrypt 같은 검증된 일방향 해시(솔트 포함) 라이브러리로 비밀번호를 저장하고, 화면·로그·URL 어디에도 평문 비밀번호가 남지 않게 하세요.

#### R-server-guard — 서버 코드 경로(API 라우트·RPC·Function)에 인증·권한 검사가 있다

| 속성 | 값 |
|---|---|
| 카테고리 | 2 |
| 중요도 | high |
| 판정 방식 | code |
| 해당없음 조건 | 서버 코드 없음 |
| 부재 증명 항목 | 아니오 |
| 근거 | 고시 제5조·제6조 + API 유출예방(권한 확인·잔존 API 점검) + 교육부 정보보안 기본지침(예규 82호) 제52조④ |

| 하위 점검 id | 내용 | required_coverage | stacks |
|---|---|---|---|
| `api-route-edge-function-no-auth` | 인증 검사 없는 API 라우트·Edge Function | code | nextjs, supabase |
| `security-definer-rpc-unverified` | `security definer` RPC의 `auth.uid()` 미검증 | code | supabase |
| `cors-wildcard` | CORS `*` 응답 | code | all |
| `stale-test-endpoint` | 방치된 테스트·구버전 엔드포인트(개편 후 외부 호출 가능한 API 잔존) | code | all |

**왜 위험한가** — 서버 API가 요청자를 확인하지 않으면, 로그인 여부와 상관없이 아무나 서버 기능을 직접 호출해 데이터를 보거나 바꿀 수 있습니다.

**수정 방법** — 모든 API·서버 함수 시작 부분에서 로그인·권한을 확인하도록 만들고, 더 이상 쓰지 않는 옛날 API는 삭제하거나 접근을 막으세요.

#### S-teacher-gate — 학생이 교사 기능을 쓸 수 없다 (역할 상승)

| 속성 | 값 |
|---|---|
| 카테고리 | 2 |
| 중요도 | medium |
| 판정 방식 | scanner, code |
| 해당없음 조건 | 교사 전용 기능 없음 |
| 부재 증명 항목 | 아니오 |
| 근거 | 고시 제5조①(최소 권한) + KISA JS 시큐어코딩 2절 1·2·3 |

| 하위 점검 id | 내용 | required_coverage | stacks |
|---|---|---|---|
| `client-password-constant` | 클라이언트 비밀번호 상수(`pw === '1234'`) | scanner | all |
| `local-storage-role-flag` | localStorage/sessionStorage 권한 플래그 | scanner | all |
| `url-hash-query-branch-only` | URL 해시·쿼리 분기만 | code | all |
| `teacher-api-no-auth` | 교사용 API 무인증 | code | all |

**왜 위험한가** — 교사 전용 기능을 여는 열쇠가 화면 코드 안에 그대로 있으면(비밀번호 상수 등), 학생이 개발자 도구로 코드를 열어보고 교사 화면에 들어갈 수 있습니다.

**수정 방법** — 교사 권한 확인은 화면(클라이언트) 코드가 아니라 서버에서 로그인 계정의 역할을 확인하는 방식으로 만드세요.

#### S-signup-scope — 입장·가입이 통제된다 (입장)

| 속성 | 값 |
|---|---|
| 카테고리 | 2 |
| 중요도 | medium |
| 판정 방식 | code, teacher |
| 해당없음 조건 | 사용자별·반별 데이터 없음 (입장 절차 없음은 na가 아니라 익명 입장으로 판정) |
| 부재 증명 항목 | 아니오 |
| 근거 | 고시 제5조④(개인정보취급자별 계정 발급·취급자 간 공유 금지 — 교사·관리자 계정에만 적용) + 고시 제6조 |

| 하위 점검 id | 내용 | required_coverage | stacks |
|---|---|---|---|
| `domain-restriction-invite-code` | 도메인 제한·승인·초대 코드 | code | all |
| `open-signup-with-auth-rule-only` | 오픈 가입 + `auth != null` 규칙 조합 | code | all |
| `no-entry-procedure-user-data-created` | 입장 절차 없이 사용자 데이터 생성 | code | all |
| `teacher-admin-account-sharing` | 교사·관리자 계정 공유(고시 제5조④는 개인정보취급자별 계정 발급·취급자 간 공유 금지 — 학생 공용 코드는 S-access) | teacher | all |

**왜 위험한가** — 아무나 가입할 수 있거나 교사·관리자 계정을 여러 명이 나눠 쓰면, 누가 어떤 데이터를 다뤘는지 책임을 가리기 어렵고 계정이 새어나갈 위험도 커집니다.

**수정 방법** — 학교 이메일 도메인 제한, 승인제, 초대 코드 등으로 가입을 통제하고, 교사·관리자 계정은 각자 따로 만들어 쓰도록 안내하세요.

#### S-auth-hardening — 인증·세션이 무차별 대입과 방치에 대비되어 있다

| 속성 | 값 |
|---|---|
| 카테고리 | 2 |
| 중요도 | medium |
| 판정 방식 | code, evidence, teacher |
| 해당없음 조건 | 로그인·입장 절차 없음 |
| 부재 증명 항목 | 아니오 |
| 근거 | ① 고시 제5조⑥(일정 횟수 인증 실패 시 접근 제한) ② 고시 제6조④(개인정보취급자가 일정시간 미사용 시 자동 접속 차단 — 취급자 대상 조항) ③ 학생 세션 만료 = KISA JS 시큐어코딩 2절 16 권고(법적 의무 아님) |

| 하위 점검 id | 내용 | required_coverage | stacks |
|---|---|---|---|
| `auth-failure-rate-limit` | 인증 실패 횟수 제한(고시 제5조⑥ — 정보주체·취급자 모두 대상). 콘솔 설정은 저장소 밖이라 증거 요청 | code, evidence | all |
| `staff-session-auto-lock` | 개인정보취급자(교사·관리자) 세션의 일정시간 미사용 자동 차단(고시 제6조④ — 이 조항은 취급자 대상) | code, teacher | all |
| `student-session-expiry` | 학생 세션 만료·자동 로그아웃(KISA 권고 근거 — 법적 의무 아님) | code | all |

**왜 위험한가** — 로그인 시도 횟수를 제한하지 않거나 교사 계정이 오래 로그인된 채로 방치되면, 계정을 알아내려는 시도나 방치된 세션을 통해 개인정보에 접근할 위험이 커집니다.

**수정 방법** — 로그인 실패가 여러 번 반복되면 잠시 막는 기능을 두고, 교사·관리자 계정은 일정 시간 사용하지 않으면 자동으로 로그아웃되게 설정하세요.

#### S-api-overfetch — 서버 응답에 화면에서 쓰지 않는 개인정보가 포함되지 않는다

| 속성 | 값 |
|---|---|
| 카테고리 | 2 |
| 중요도 | medium |
| 판정 방식 | scanner, code |
| 해당없음 조건 | 서버 응답에 개인정보 없음 |
| 부재 증명 항목 | 아니오 |
| 근거 | API 유출예방(2026.7) "이용자 화면에서 보이지 않는 개인정보나 서비스 제공 목적에 필요하지 않은 정보는 API 응답 데이터에서도 제외" |

| 하위 점검 id | 내용 | required_coverage | stacks |
|---|---|---|---|
| `select-all-columns` | `select('*')`·전체 컬럼 반환 (스캐너 hit = 후보만) | scanner | supabase |
| `list-api-exposes-other-identity` | 목록 API가 타인의 식별정보까지 반환 | code | all |
| `unused-field-in-response` | 화면 미사용 필드 포함 — AI가 응답 스키마와 화면 사용 필드를 추적해 대조한 경우에만 fail(뷰·RPC가 이미 필드를 제한하거나 전 필드를 실제로 쓰는 경우 오탐) | code | all |

**왜 위험한가** — 화면에 보여줄 필요가 없는 개인정보까지 서버 응답에 포함되면, 개발자 도구로 통신 내용을 열어본 사람에게 정보가 그대로 드러날 수 있습니다.

**수정 방법** — 서버에서 데이터를 조회할 때 `select('*')`처럼 전체 컬럼을 가져오지 말고, 화면에 실제로 필요한 항목만 골라서 응답하도록 만드세요.

#### S-write-guard — 쓰기 규칙에 크기·형식 검증이 있다

| 속성 | 값 |
|---|---|
| 카테고리 | 2 |
| 중요도 | medium |
| 판정 방식 | evidence |
| 해당없음 조건 | 외부 DB 미사용 |
| 부재 증명 항목 | 아니오 |
| 근거 | 고시 제6조 + 개발보안 가이드(입력 검증) |

| 하위 점검 id | 내용 | required_coverage | stacks |
|---|---|---|---|
| `size-type-check` | `size()`·타입 검사 | evidence | firebase |
| `rls-with-check-condition` | RLS `with check` 조건 | evidence | supabase |

**왜 위험한가** — 저장되는 데이터의 크기나 형식을 확인하지 않으면, 지나치게 큰 데이터나 예상 밖의 형식이 들어와 서비스가 오작동하거나 저장 공간이 낭비될 수 있습니다.

**수정 방법** — 보안 규칙에 데이터 크기 제한과 필드 타입 검사를 추가해서, 정해진 형식과 크기를 벗어나는 쓰기는 거부되게 하세요.

### 6.3 카테고리 3. 비밀·파일 노출 — 저장소·번들·히스토리에 뭐가 들어 있나

#### R-secrets — 비밀키가 소스·git 히스토리·빌드 산출물 어디에도 없다

| 속성 | 값 |
|---|---|
| 카테고리 | 3 |
| 중요도 | high |
| 판정 방식 | scanner |
| 해당없음 조건 | — (부재 증명) |
| 부재 증명 항목 | 예 |
| 근거 | 법 제29조 + 고시 제7조 + KISA JS 시큐어코딩 2절 6·13(주석 속 중요정보) |

| 하위 점검 id | 내용 | required_coverage | stacks |
|---|---|---|---|
| `key-pattern-match` | 키 패턴(sk-, AIza, AKIA, ghp_, Supabase service_role JWT, 개인키, 하드코딩 비밀번호) | scanner | all |
| `secret-in-comment` | 주석문 안의 비밀정보 | scanner | all |
| `env-file-committed` | `.env` 커밋 | history | all |
| `public-prefix-secret` | `NEXT_PUBLIC_`/`VITE_`/`REACT_APP_` 접두사에 시크릿 | scanner | nextjs, vite-react |
| `build-artifact-key` | 빌드 산출물 내 키 | build | all |

**왜 위험한가** — API 키나 비밀번호가 코드나 git 기록에 남아 있으면, 그 저장소를 볼 수 있는 누구나 그 키로 여러분의 서비스·데이터에 접근할 수 있게 됩니다.

**수정 방법** — 키는 환경변수로 분리하고 `.gitignore`에 등록하세요. 이미 노출된 키는 삭제 커밋만으로는 안전해지지 않으니 반드시 재발급(rotate)하세요.

#### R-admin-data — 학생 데이터 파일(명단·성적 csv/xlsx/json)이 저장소·배포물에 없다

| 속성 | 값 |
|---|---|
| 카테고리 | 3 |
| 중요도 | high |
| 판정 방식 | scanner, code, teacher |
| 해당없음 조건 | — (부재 증명) |
| 부재 증명 항목 | 예 |
| 근거 | 고시 제6조③ + 교육부 예규 82호 제68조(비공개 업무자료 홈페이지 무단 게시 금지) |

| 하위 점검 id | 내용 | required_coverage | stacks |
|---|---|---|---|
| `name-studentid-contact-columns` | 이름·학번·연락처 열 | scanner | all |
| `seed-sample-real-student-name` | 시드·샘플 데이터의 실제 학생 이름 | scanner, code | all |
| `build-artifact-inclusion` | 배포물 포함 여부 | build | all |
| `repo-visibility` | repo 공개 여부(원격 URL·gh 감지, 실패 시 확인) | code, teacher | all |
| `gitignore-handling` | `.gitignore` 처리 | scanner | all |

**왜 위험한가** — 학생 명단·성적 파일이 저장소에 그대로 들어 있으면, 특히 공개 저장소일 때 누구나 다운로드해 학생 개인정보를 통째로 볼 수 있습니다.

**수정 방법** — 실제 학생 이름·학번이 담긴 파일은 저장소에 올리지 말고, 예시 데이터는 가짜 이름으로 바꾸세요. 이미 올라간 파일은 `.gitignore`에 추가하고 git 기록에서도 제거하세요.

#### S-answer-exposure — 평가·경쟁 문항의 정답·채점 기준이 클라이언트에 없다

| 속성 | 값 |
|---|---|
| 카테고리 | 3 |
| 중요도 | medium |
| 판정 방식 | scanner, code |
| 해당없음 조건 | 정답 공개가 무해한 연습용 |
| 부재 증명 항목 | 아니오 |
| 근거 | 평가 공정성 — 법령 근거 없음, 실측 사고 기반 모범 사례 + JS 시큐어코딩 캡슐화 2(디버그 코드 잔존) |

| 하위 점검 id | 내용 | required_coverage | stacks |
|---|---|---|---|
| `static-json-answer-field` | 정적 JSON 정답 필드 | scanner | all |
| `bundle-answer-grading-keyword` | 번들 내 정답·채점 키워드·서버 전용 표식 | build | all |
| `leftover-debug-code` | 제거되지 않고 남은 디버그 코드(정답 표시·개발용 우회) | scanner, build | all |

**왜 위험한가** — 정답이나 채점 기준이 학생 화면 쪽 코드(클라이언트)에 그대로 있으면, 개발자 도구로 코드를 열어본 학생이 정답을 미리 알 수 있습니다.

**수정 방법** — 정답·채점 로직은 서버에서만 처리하고, 학생 화면에는 결과만 전달하세요. 테스트용으로 남겨둔 정답 표시 기능은 배포 전에 제거하세요.

### 6.4 카테고리 4. 제3자 전송·추적 — 데이터가 어디로 나가나

목적지 분류(4a `destinations[]`): **수탁자(processor)** = 앱 동작에 필수인 저장·호스팅·인증 서비스(Firebase·Supabase·Vercel 등). 법적으로 **처리 위탁**이며 국외일 수 있어 **처리방침 고지 대상**이다(허용이되 면책이 아님). **독립 제3자(third_party)** = 외부 AI API·분석·광고·메일·Google Forms/Sheets·웹훅. **미상(unknown)** = 판별하지 못한 목적지.

#### R-third-party — 학생 식별·민감 정보가 독립 제3자로 전송되지 않는다

| 속성 | 값 |
|---|---|
| 카테고리 | 4 |
| 중요도 | high |
| 판정 방식 | scanner, code |
| 해당없음 조건 | 학생 식별 정보 미처리 |
| 부재 증명 항목 | 아니오 |
| 근거 | 법 제17조·제26조·제28조의8 + 교육청 생성형 AI 지침 |

| 하위 점검 id | 내용 | required_coverage | stacks |
|---|---|---|---|
| `identifiable-data-to-external-service` | 외부 AI API·분석/광고 SDK·외부 메일·웹훅에 실명·학번·연락처·민감정보가 실리는 경로 | scanner, code | all |
| `google-forms-sheets-sharing-scope` | Google Forms/Sheets/Apps Script 전송과 시트 공유 범위 | code | all |
| `unknown-destination` | 미상 목적지(수탁자로의 전송은 이 항목 위반이 아니며 고지는 S-privacy-notice, 국외 저장은 S-data-region) | code | all |

**왜 위험한가** — 학생의 이름·학번·연락처가 외부의 광고·분석 서비스 등으로 그대로 전송되면, 처음에 알린 목적을 벗어난 방식으로 쓰일 수 있습니다.

**수정 방법** — 외부로 보내는 데이터에서 이름·학번 같은 식별 정보는 빼거나 임의 코드로 바꾸고, 꼭 필요한 경우가 아니면 외부 서비스에 개인정보를 보내지 마세요.

#### R-llm-input — 학생 입력이 외부 AI로 전송되면 그 사실을 고지하고 개인정보 입력 금지를 안내한다

| 속성 | 값 |
|---|---|
| 카테고리 | 4 |
| 중요도 | medium |
| 판정 방식 | scanner, code |
| 해당없음 조건 | 외부 AI 미사용 |
| 부재 증명 항목 | 아니오 |
| 근거 | 법 제15조·제26조 + 생성형 AI 개발·활용 개인정보 처리 안내서(2025) p.27(개인용 vs 기업용 API 라이선스) |

| 하위 점검 id | 내용 | required_coverage | stacks |
|---|---|---|---|
| `ai-endpoint-call` | AI 엔드포인트 호출 | scanner, code | all |
| `ai-usage-notice` | 고지 문구 | code | all |
| `pii-input-prohibition-notice` | 입력 금지 안내 | code | all |
| `ai-training-license-check` | 입력이 AI 학습에 활용되지 않는 라이선스·설정인가(개인용=학습 활용 기본·opt-out / 기업용 API=미저장·미학습 기본). 개인정보가 실제로 실리면 R-third-party 상 | code | all |

**왜 위험한가** — 학생이 입력한 내용이 외부 AI 서비스로 전송된다는 사실을 모르면, 학생이 실수로 개인정보나 고민을 그대로 입력해 외부에 전달될 수 있습니다.

**수정 방법** — AI 기능을 쓰는 화면에는 "입력 내용이 AI로 전송됩니다. 이름·연락처 등 개인정보는 입력하지 마세요" 같은 안내 문구를 넣으세요.

#### S-data-region — 학생 데이터가 저장되는 위치(리전)를 알고 있고, 학교가 도입해 쓰는 경우 국내에 저장·관리되는 서비스를 이용한다

| 속성 | 값 |
|---|---|
| 카테고리 | 4 |
| 중요도 | medium |
| 판정 방식 | code, teacher |
| 해당없음 조건 | 서버 저장 없음 |
| 부재 증명 항목 | 아니오 |
| 근거 | 교육부 예규 82호 제41조②1 — 각급기관이 민간 클라우드를 이용할 때 "국내에 위치한 정보시스템(인증서버, 로그 및 백업서버 등)·관리 주체에 의해 데이터가 저장·관리되는 서비스의 이용"을 준수해야 한다(고지 의무가 아니라 국내 저장 요건). 적용 주체는 "각급기관의 장"이므로 controller=school일 때 요건, teacher_personal은 권고 |

| 하위 점검 id | 내용 | required_coverage | stacks |
|---|---|---|---|
| `project-region-setting` | 프로젝트 리전 설정 확인(Firebase location·Supabase region) | code | all |
| `school-controller-domestic-storage` | controller=school 이면 국내 저장 요건(교육부 예규 제41조②1: "국내에 위치한 정보시스템(인증서버·로그·백업서버 등)·관리 주체에 의해 데이터가 저장·관리되는 서비스의 이용") — 국외 저장이면 fail | code | all |
| `teacher-personal-region-awareness` | controller=teacher_personal 이면 국내 저장은 권고, 리전 인지 여부만 판정. 국외 이전 고지는 이 항목이 아니라 S-privacy-notice ⑥ | teacher | all |

**왜 위험한가** — 학생 데이터가 국외 서버에 저장되면, 국내와 다른 법이 적용되어 정보를 보호받기 어려워질 수 있고, 학교에서 도입한 앱이라면 교육부가 정한 국내 저장 요건을 어기게 됩니다.

**수정 방법** — Firebase·Supabase 등에서 데이터가 저장되는 리전(지역)을 확인하고, 학교가 정식으로 도입해 쓰는 앱이라면 국내 리전을 선택하세요.

#### S-tracking — 학생용 앱에 분석·광고 추적 스크립트가 없거나 고지된다

| 속성 | 값 |
|---|---|
| 카테고리 | 4 |
| 중요도 | low |
| 판정 방식 | scanner |
| 해당없음 조건 | student_facing 아님 (부재 증명) |
| 부재 증명 항목 | 예 |
| 근거 | 아동·청소년 안내서(맞춤형 광고 최소화·행태정보) |

| 하위 점검 id | 내용 | required_coverage | stacks |
|---|---|---|---|
| `ga-gtag-ad-pixel` | GA·gtag·광고 태그·픽셀 | scanner | all |
| `behavioral-ad-targeting-minors` | 아동 대상 서비스는 맞춤형 광고 목적 행태정보 수집 금지 | scanner | all |
| `cross-site-tracking` | 다른 앱·웹에 걸친 활동 추적 불허 | scanner | all |

**왜 위험한가** — 학생이 쓰는 앱에 광고·분석 추적 스크립트가 몰래 들어 있으면, 학생의 행동 정보가 외부 광고 회사로 넘어가 맞춤형 광고 등에 쓰일 수 있습니다.

**수정 방법** — 학생용 화면에는 분석·광고 추적 스크립트를 넣지 않거나, 꼭 필요하면 어떤 정보를 어디로 보내는지 안내 문구로 밝히세요.

### 6.5 카테고리 5. 화면·로그 노출 — 눈에 어디까지 보이나

#### S-name-exposure — 공개 화면(랭킹·결과 공유·프로젝터)에 실명·학번이 노출되지 않는다

| 속성 | 값 |
|---|---|
| 카테고리 | 5 |
| 중요도 | medium |
| 판정 방식 | code |
| 해당없음 조건 | 공개 화면 없음 |
| 부재 증명 항목 | 아니오 |
| 근거 | 법 제17조(제3자 제공) + 고시 제12조(출력 항목 최소화) + 아동 안내서 1-2 |

| 하위 점검 id | 내용 | required_coverage | stacks |
|---|---|---|---|
| `public-screen-display-fields` | 공개 화면의 표시 필드 | code | all |
| `nickname-real-name-guidance` | "닉네임에 실명을 쓰라"는 안내 | code | all |
| `public-default-scope-private` | 공개 범위 기본값이 비공개인가 | code | all |
| `export-field-minimization` | 출력·내보내기 시 항목 최소화 | code | all |

**왜 위험한가** — 랭킹판·프로젝터 화면 같은 공개된 곳에 실명·학번이 그대로 표시되면, 그 자리에 있는 모든 사람이 학생의 개인정보를 알게 됩니다.

**수정 방법** — 공개 화면에는 실명 대신 닉네임을 쓰고, 화면 설계 단계부터 공개 범위를 기본적으로 좁게(비공개) 설정하세요.

#### S-log-pii — 개인정보를 콘솔·에러 리포팅·서버 로그에 출력하지 않는다

| 속성 | 값 |
|---|---|
| 카테고리 | 5 |
| 중요도 | medium |
| 판정 방식 | scanner, code |
| 해당없음 조건 | 개인정보 미처리 (부재 증명) |
| 부재 증명 항목 | 예 |
| 근거 | 법 제29조 + 고시 제6조③ + JS 시큐어코딩 에러처리 1 |

| 하위 점검 id | 내용 | required_coverage | stacks |
|---|---|---|---|
| `console-log-user-object` | `console.log`에 사용자 객체·이름 | scanner | all |
| `error-reporting-payload` | Sentry 등 리포팅 페이로드 | scanner | all |
| `error-message-info-leak` | 오류 메시지 정보 노출(스택 트레이스·DB 오류를 사용자 화면에 표시) | code | all |

**왜 위험한가** — 개발 중에 확인용으로 남긴 로그에 학생 이름·정보가 찍히면, 로그를 볼 수 있는 사람이나 로그 서비스로 개인정보가 새어나갈 수 있습니다.

**수정 방법** — 배포 전에 `console.log`에서 개인정보를 출력하는 코드를 지우고, 오류가 나도 학생 화면에는 자세한 오류 내용 대신 안내 메시지만 보여주세요.

#### S-shared-device — 공용 기기에서 이전 사용자 정보가 남지 않는다

| 속성 | 값 |
|---|---|
| 카테고리 | 5 |
| 중요도 | medium |
| 판정 방식 | code |
| 해당없음 조건 | 개인 데이터 없음 |
| 부재 증명 항목 | 아니오 |
| 근거 | 아동·청소년 안내서 + JS 시큐어코딩 2절 12 |

| 하위 점검 id | 내용 | required_coverage | stacks |
|---|---|---|---|
| `localstorage-pii-token` | localStorage 개인정보·토큰 | code | all |
| `reset-logout-mechanism` | 초기화·로그아웃 수단 | code | all |
| `cookie-persisted-info` | 하드디스크에 남는 쿠키를 통한 정보 노출 | code | all |

**왜 위험한가** — 학교 컴퓨터처럼 여러 학생이 함께 쓰는 기기에 이전 학생의 이름·로그인 정보가 남아 있으면, 다음 사용자가 그 정보를 볼 수 있습니다.

**수정 방법** — 로그아웃 시 저장된 개인정보·토큰을 확실히 지우고, 공용 기기에서 쓰기 쉽도록 눈에 띄는 로그아웃·초기화 버튼을 두세요.

#### S-rank-optout — 랭킹·비교 기능에 특정 학생 제외·마스킹 수단이 있다

| 속성 | 값 |
|---|---|
| 카테고리 | 5 |
| 중요도 | low |
| 판정 방식 | code |
| 해당없음 조건 | 랭킹 없음 |
| 부재 증명 항목 | 아니오 |
| 근거 | 법령 근거 없음 — 실측 사고 기반 모범 사례 |

| 하위 점검 id | 내용 | required_coverage | stacks |
|---|---|---|---|
| `exclude-mask-option` | 제외·마스킹 옵션 | code | all |

**왜 위험한가** — 원치 않는 학생까지 랭킹에 강제로 노출되면, 성적이나 순위가 공개되는 것이 부담스러운 학생에게 불편이나 위축감을 줄 수 있습니다.

**수정 방법** — 랭킹·비교 화면에 특정 학생을 빼거나 이름을 가릴 수 있는 옵션을 만들어, 교사가 필요할 때 조정할 수 있게 하세요.

### 6.6 카테고리 6. 코드 안전

#### S-injection — 입력이 코드·쿼리로 해석되는 경로가 없다 (KISA 분류: 입력데이터 검증 및 표현)

| 속성 | 값 |
|---|---|
| 카테고리 | 6 |
| 중요도 | medium |
| 판정 방식 | scanner, code |
| 해당없음 조건 | — (부재 증명) |
| 부재 증명 항목 | 예 |
| 근거 | KISA 소프트웨어 개발보안 가이드(입력데이터 검증 및 표현) + 교육부 예규 82호 제27조 |

| 하위 점검 id | 내용 | required_coverage | stacks |
|---|---|---|---|
| `innerhtml-eval-variable-injection` | `innerHTML`/`eval`/`document.write`에 변수 삽입 | scanner | all |
| `dangerously-set-inner-html` | `dangerouslySetInnerHTML` | scanner | all |
| `server-sql-string-concat` | 서버 코드의 SQL 문자열 결합 | scanner, code | all |

**왜 위험한가** — 사용자가 입력한 값이 그대로 화면 코드나 데이터베이스 쿼리로 실행되면, 의도하지 않은 코드가 함께 실행되어 화면이 조작되거나 데이터가 잘못 바뀔 수 있습니다.

**수정 방법** — 사용자 입력을 `innerHTML`이나 SQL 문자열에 직접 끼워 넣지 말고, 프레임워크가 제공하는 안전한 방식(텍스트 삽입, 파라미터 바인딩)을 쓰세요.

#### S-abuse-limit — 외부 리소스(DB·AI API) 남용 제한이 있다

| 속성 | 값 |
|---|---|
| 카테고리 | 6 |
| 중요도 | medium |
| 판정 방식 | scanner, code |
| 해당없음 조건 | 외부 DB·AI 미사용 |
| 부재 증명 항목 | 아니오 |
| 근거 | API 유출예방 "API로 대규모 정보 조회 또는 반복 호출 등을 제한" (기존 "근거 없음"에서 격상) |

| 하위 점검 id | 내용 | required_coverage | stacks |
|---|---|---|---|
| `db-direct-app-check` | DB 직접 경로: App Check·reCAPTCHA | code | all |
| `ai-proxy-auth-rate-limit` | 서버 AI 프록시: 인증·호출 제한 | code | all |
| `bulk-query-repeat-call-limit` | 대규모 조회·반복 호출 제한 | code | all |

**왜 위험한가** — 데이터베이스나 AI API 호출 횟수를 제한하지 않으면, 짧은 시간에 지나치게 많은 요청이 몰려 서비스 이용료가 크게 나오거나 서비스가 느려질 수 있습니다.

**수정 방법** — App Check·reCAPTCHA 같은 남용 방지 도구를 달거나, AI 호출은 서버를 거치게 해서 횟수·빈도를 제한하세요.

#### S-https — 모든 리소스·전송이 https다

| 속성 | 값 |
|---|---|
| 카테고리 | 6 |
| 중요도 | low |
| 판정 방식 | scanner |
| 해당없음 조건 | — (부재 증명) |
| 부재 증명 항목 | 예 |
| 근거 | 고시 제7조④ "인터넷망 구간으로 송·수신하는 경우 암호화" |

| 하위 점검 id | 내용 | required_coverage | stacks |
|---|---|---|---|
| `http-resource-endpoint` | `http://` 리소스·엔드포인트 (localhost 제외) | scanner | all |

**왜 위험한가** — http로 데이터를 주고받으면 암호화되지 않은 채로 전송되어, 같은 네트워크에 있는 다른 사람이 통신 내용을 들여다볼 수 있습니다.

**수정 방법** — 모든 이미지·API·리소스 주소를 http가 아닌 https로 바꾸세요. 배포 시 대부분의 호스팅 서비스는 https를 기본 제공합니다.

### 6.7 카테고리 7. 고지·보유·파기 — 알리고, 지키고, 지우나

#### S-privacy-notice — 개인정보 처리 안내(처리방침)가 있고 법정 기재사항을 담는다

| 속성 | 값 |
|---|---|
| 카테고리 | 7 |
| 중요도 | medium |
| 판정 방식 | code, teacher |
| 해당없음 조건 | 개인정보 미수집 |
| 부재 증명 항목 | 아니오 |
| 근거 | 법 제30조① + 시행령 제31조① + 개인정보 처리방침 작성지침(2026.4) 기재사항 24개 + 표준지침 제18조(알기 쉬운 용어) |

| 하위 점검 id | 내용 | required_coverage | stacks |
|---|---|---|---|
| `purpose-retention-period` | 수집 시점 항목·목적·보유 기간 | code | all |
| `destruction-procedure` | 파기 절차 및 방법 | code | all |
| `under14-handling-if-applicable` | 만 14세 미만 처리 사항(해당시) | code | all |
| `third-party-provision-if-applicable` | 제3자 제공(해당시) | code | all |
| `outsourcing-processor-disclosure` | 위탁(수탁자 = Firebase·Supabase·Vercel 등 명시) | code | all |
| `overseas-transfer-if-applicable` | 국외 이전(해당시 — 리전 판정은 S-data-region) | code | all |
| `safety-measures-disclosure` | 안전성 확보조치에 관한 사항 | code | all |
| `cookie-and-optout` | 자동 수집 장치(쿠키)와 거부 방법(해당시) | code | all |
| `data-subject-rights-procedure` | 권리(열람·정정·삭제·처리정지·동의철회) 행사 방법 | code, teacher | all |
| `controller-contact-info` | 보호책임자·연락처 — 프로필의 `controller`에 따라: school이면 학교·교장 또는 학교가 지정한 창구(교육부 훈령 제5조②4: 유·초·중·고 보호책임자 = 원장 또는 교장), teacher_personal이면 실제 개인 운영 주체와 연락 수단, unknown이면 확인 질문 | code, teacher | all |
| `amendment-and-plain-language` | 변경에 관한 사항 · 알기 쉬운 용어로 작성 | code | all |

**왜 위험한가** — 처리방침이 없거나 필수 항목이 빠져 있으면, 학생·학부모가 어떤 정보가 왜 모이고 언제 지워지는지 알 수 없고, 법에서 정한 고지 의무도 어기게 됩니다.

**수정 방법** — 수집 항목·목적·보유기간·파기방법·연락처 등을 담은 처리방침 페이지를 만들어 앱 안에서 쉽게 찾을 수 있게 링크하세요.

#### H-delete — 학생 1명 단위로 삭제할 수단이 있고 파생 데이터도 함께 지워진다

| 속성 | 값 |
|---|---|
| 카테고리 | 7 |
| 중요도 | medium |
| 판정 방식 | code, teacher |
| 해당없음 조건 | 서버 저장 없음 |
| 부재 증명 항목 | 아니오 |
| 근거 | 법 제21조·제36조 + 고시 제13조(복구되지 않도록 초기화·덮어쓰기) + 아동 안내서 4-2(5일 이내) |

| 하위 점검 id | 내용 | required_coverage | stacks |
|---|---|---|---|
| `deletion-mechanism` | 삭제 수단 | code | all |
| `derived-data-cascade` | 진행도·기록·랭킹 등 파생 데이터 | code | all |
| `irreversible-destruction` | 복구·재생 불가능한 파기(soft delete 플래그만이면 파기 아님) | code | all |
| `purge-within-5-days` | 불필요하게 된 정보는 5일 이내 파기 | code, teacher | all |

**왜 위험한가** — 학생을 삭제해도 점수·기록·랭킹 같은 관련 데이터가 남아 있으면, 삭제 요청을 받아도 실제로는 개인정보가 계속 남아 있는 셈이 됩니다.

**수정 방법** — 학생 삭제 시 그 학생과 관련된 진행도·기록·랭킹 데이터까지 함께 지우고, 복구할 수 없도록 완전히 삭제되게 만드세요.

#### H-2fa — 운영 계정(GitHub·클라우드)에 2단계 인증이 켜져 있다

| 속성 | 값 |
|---|---|
| 카테고리 | 7 |
| 중요도 | medium |
| 판정 방식 | teacher |
| 해당없음 조건 | 해당없음 조건 없음 — 운영 계정이 있는 한 항상 해당 |
| 부재 증명 항목 | 아니오 |
| 근거 | 고시 제5조⑤·제6조② |

| 하위 점검 id | 내용 | required_coverage | stacks |
|---|---|---|---|
| `admin-account-2fa` | GitHub·Firebase·Supabase 등 운영 계정의 2단계 인증 활성화 | teacher | all |

**왜 위험한가** — 운영 계정이 뚫리면(비밀번호가 새어나가면) 서비스 전체와 학생 데이터에 접근할 수 있게 되는데, 2단계 인증이 없으면 비밀번호만으로 계정이 넘어갈 수 있습니다.

**수정 방법** — GitHub·Firebase·Supabase 등 관리자 계정 설정에서 2단계 인증(OTP 앱, 보안키 등)을 켜세요.

#### H-retention — 활동 종료 후 데이터 파기 계획이 있다

| 속성 | 값 |
|---|---|
| 카테고리 | 7 |
| 중요도 | low |
| 판정 방식 | teacher |
| 해당없음 조건 | 서버 저장 없음 |
| 부재 증명 항목 | 아니오 |
| 근거 | 법 제21조 + 교육부 개인정보 보호지침(훈령 476호) [별표1] 보유기간 책정 기준표 |

| 하위 점검 id | 내용 | required_coverage | stacks |
|---|---|---|---|
| `destruction-plan` | 파기 계획 | teacher | all |
| `school-year-transition-handling` | 학년도 전환 시 처리 | teacher | all |
| `retention-period-basis` | 보유기간 산정(교육부 [별표1] 기준표: 단순 보고 1년 / 행정 참고 3년 등) | teacher | all |

**왜 위험한가** — 활동이 끝난 뒤에도 학생 데이터를 계속 갖고 있으면, 더 이상 필요 없는 정보가 불필요하게 오래 남아 유출 위험이 계속됩니다.

**수정 방법** — 학기·학년이 끝나면 언제까지 데이터를 보관하고 언제 지울지 미리 정해두고, 그 시점이 되면 실제로 삭제하세요.

#### H-breach-ready — 유출 사고 대응 체계가 준비되어 있다

| 속성 | 값 |
|---|---|
| 카테고리 | 7 |
| 중요도 | medium |
| 판정 방식 | teacher |
| 해당없음 조건 | 개인정보 미처리 |
| 부재 증명 항목 | 아니오 |
| 근거 | 교육부 훈령 476호 제13조①(1명이라도 유출을 알게 된 때 72시간 내 상급기관 경유 교육부 신고, 1천명·민감정보·외부 불법접근은 개인정보위 동시 신고) + 제13조②(경로 확인 후 회수·삭제로 권익침해 가능성이 현저히 낮아진 경우 신고 예외) + 법 제34조 |

| 하위 점검 id | 내용 | required_coverage | stacks |
|---|---|---|---|
| `breach-report-contacts` | 보고 경로: 상급기관(교육지원청·교육청) 연락처와 학교 개인정보 담당자를 알고 있는가 | teacher | all |
| `breach-72h-timer-start` | 시한 기산점: 유출을 알게 된 때로부터 72시간(교육부 훈령 제13조① — 1명이라도 원칙 신고) | teacher | all |
| `breach-pipc-notification-conditions` | 개인정보위 동시 신고 조건: 1천명 이상 / 민감정보·고유식별정보 / 외부 불법 접근 | teacher | all |
| `breach-exception-awareness` | 예외 인지: 유출 경로가 확인되어 회수·삭제 등으로 권익침해 가능성이 현저히 낮아지면 신고하지 않을 수 있다(제13조② — 판단은 학교 개인정보 담당자·보호책임자 몫, 교사 임의 판단 아님) | teacher | all |
| `breach-initial-response-plan` | 초동조치: 접근 차단·증거 보전·정보주체 통지 계획 | teacher | all |

**왜 위험한가** — 유출 사고가 났을 때 누구에게 알려야 하는지, 언제까지 신고해야 하는지 모르면 초기 대응이 늦어져 피해가 더 커지고 신고 의무도 놓칠 수 있습니다.

**수정 방법** — 학교 개인정보 담당자·교육지원청 연락처를 미리 알아두고, 유출을 알게 되면 72시간 안에 신고해야 한다는 절차를 기억해 두세요.

#### H-school-approval — 정규 교과 사용 앱이라면 학교운영위원회 심의를 거쳤다

| 속성 | 값 |
|---|---|
| 카테고리 | 7 |
| 중요도 | low |
| 판정 방식 | teacher |
| 해당없음 조건 | 정규 교과 미사용, 또는 아래 두 트리거 모두 비해당 |
| 부재 증명 항목 | 아니오 |
| 근거 | 「초·중등교육법」 제29조의2(2025-08-14 신설, 2026-03-01 시행) + 교육부 「학습지원 소프트웨어 선정 기준 및 가이드라인」(2025.12) 적용대상 ①②(각주: 교사 제작 콘텐츠는 ②에서 제외) |

| 하위 점검 id | 내용 | required_coverage | stacks |
|---|---|---|---|
| `trigger-pii-processing` | 적용 트리거: 학생 개인정보를 수집·이용·제공 등 처리 | teacher | all |
| `trigger-curriculum-content-provider` | 적용 트리거: 교과 성취기준 관련 학습콘텐츠를 포함하며 콘텐츠 제공 기관이 교육과정 운영 지원 목적으로 개발·보급(교육부 기준 각주 — "소프트웨어를 통해 교사가 제작한 콘텐츠를 포함하는 경우 등은 제외"이므로 교사 자작 앱은 대개 이 트리거에 해당하지 않음) | teacher | all |
| `review-obligation-awareness` | 「초·중등교육법」 제29조의2(2026-03-01 시행) 심의 대상 여부 인지 | teacher | all |
| `moe-checklist-not-substitute` | 보고서의 [서식 1] 대조표는 심의·서식을 대체하지 않는 작성 참고자료 | teacher | all |

**왜 위험한가** — 정규 교과 수업에 쓰는 앱이 학교운영위원회 심의를 거치지 않으면, 학교 차원의 정식 절차 없이 학생들에게 사용되어 법이 정한 절차를 지키지 못하게 됩니다.

**수정 방법** — 정규 수업에 쓸 계획이라면 미리 학교운영위원회 심의 대상인지 확인하고, 필요한 경우 심의를 신청하세요.

### 6.8 카테고리 8. 학생 안전

#### R-crisis — 감정·고민 입력 기능이 있으면 위기 안내(교사·1388)가 있다

| 속성 | 값 |
|---|---|
| 카테고리 | 8 |
| 중요도 | high |
| 판정 방식 | code |
| 해당없음 조건 | 그런 기능 없음 |
| 부재 증명 항목 | 아니오 |
| 근거 | 교육분야 인공지능 윤리원칙(안전 보장) + 청소년상담 1388 |

| 하위 점검 id | 내용 | required_coverage | stacks |
|---|---|---|---|
| `counseling-emotion-input-ui` | 상담·감정 입력 UI | code | all |
| `crisis-guidance-message` | 위기 안내 문구 | code | all |

**왜 위험한가** — 학생이 힘든 감정이나 고민을 입력하는 기능이 있는데 위기 상황에 도움을 요청할 안내가 없으면, 정말 도움이 필요한 학생이 어디에 연락해야 할지 몰라 넘어갈 수 있습니다.

**수정 방법** — 감정·고민을 입력하는 화면에 "힘들 때는 선생님이나 청소년상담 1388로 연락하세요" 같은 안내 문구를 넣으세요.

## 7. 판정 규칙

### 7.1 판정값과 출처

| 판정값 | 뜻 |
|---|---|
| `pass` / `fail` / `na` / `needs_human` | 충족 / 미충족 / 해당없음 / 판단불가 |

`[REQ-7.1]` 판정값은 이 4개뿐이다. 5번째 값을 두지 않고 사유(`reason`)로 구분한다(트랙 2 호환).

| 출처 (`sources[]`) | 뜻 | 보고서 표기 |
|---|---|---|
| `scanner` | 동봉 스크립트의 결정적 결과 (`negative_scan` 포함) | 충족 / 미충족 |
| `code` | AI 가 코드를 추적해 판정 | 충족 / 미충족 |
| `evidence` | 교사가 붙여넣은 규칙·설정으로 판정 | 충족 (제출 증거) |
| `teacher` | 교사 답변에 근거 | **교사 확인:** 충족 |

`[REQ-7.2]` `verification_level` 은 `verified`(scanner/code/evidence) · `attested`(teacher) · `none` 셋 중 하나다. 종합 집계에서 `attested` pass 는 `verified` pass 와 섞지 않고 별도로 센다.

`[REQ-7.25]` `verdict` 가 `na` 이거나 `needs_human` 이면 검증 주체가 없으므로 `verification_level` 은 `none` 이다. 항목과 하위 점검 모두에 적용된다.

### 7.2 우선순위와 집계

`[REQ-7.3]` 출처가 충돌하면 다음 순서로 최악을 택한다: **검증된 fail > needs_human > 교사확인 pass > 검증된 pass > na**.

`[REQ-7.4]` 교사 답변은 `needs_human` 만 대체할 수 있다. 스캐너·코드·증거로 확정된 `fail` 은 교사 답변으로 해제되지 않는다.

`[REQ-7.5]` 하위 점검이 있는 항목의 판정은 REQ-7.3 의 순서로 하위 점검 중 최악이다. `na` 인 하위 점검은 집계에서 제외하되, 전부 `na` 면 항목도 `na` 다.

`[REQ-7.6]` `required_coverage` 가 확보되지 않은 하위 점검은 `needs_human`(사유 기록)이고, 따라서 항목도 `needs_human` 이다.

### 7.3 신뢰성 규칙

`[REQ-7.7]` `pass`·`fail` 에는 근거가 1개 이상 필수다 — 인용(파일·줄·내용) 또는 `negative_scan`(규칙·범위). 없으면 `needs_human` 으로 강등하고 `demotion_reason` 을 기록한다.

`[REQ-7.8]` 37개 항목 전부가 보고서에 나타난다. 누락은 `needs_human` 으로 채운다.

`[REQ-7.9]` 확신이 없으면 `fail` 이 아니라 `needs_human` 으로 남기고 확인 세션 후보로 돌린다(과잉 판정 금지).

`[REQ-7.10]` `na` 에는 `applicability_reason` 이 필수다.

`[REQ-7.11]` 비밀키·개인정보 인용은 **앞 6자 + `****`** 로 마스킹한다. `scan.json`·`edusafe-report.json`·HTML·MD **모든 산출물에 공통 적용**하며, 마스킹은 저장하기 전 단계에서 이루어진다.

`[REQ-7.12]` 인용은 200자 이내, 항목당 근거는 4개 이내다.

`[REQ-7.13]` 같은 근거를 여러 항목이 인용할 수 있다. 보고서 요약에서는 finding 단위로 중복 표시만 접는다.

### 7.4 문서 파일 hit 의 취급

스캔 대상에는 `.md`·`.txt` 나 `docs/`·`.claude/`·`.agents/` 경로의 문서가 섞인다. 문서에 적힌 예제 코드를 실제 취약점으로 판정하면 오탐이고, 반대로 README 에 진짜 키를 적는 사고는 실제로 흔하다. 그래서 둘을 나눈다.

`[REQ-7.14]` `scan.mjs` 는 hit 마다 `documentation` 플래그(문서 파일 여부)를 기록한다.

`[REQ-7.15]` 시크릿 계열 규칙(§9 에서 `secretValue` 플래그가 있는 규칙)의 hit 은 `documentation: true` 여도 그대로 판정 근거로 쓴다.

`[REQ-7.16]` 그 외 규칙의 hit 이 `documentation: true` 면 판정 근거로 쓰지 않는다. 다만 **집계에서 조용히 사라지지 않게** 보고서에 "문서에서 발견(참고)"으로 별도 표시하고 그 건수를 요약에 남긴다.

### 7.5 확인 세션 정책 (7단계 — 보고서가 이미 완성된 뒤 선택적으로 진행)

확인 세션은 두 종류다. **증거 제출형**은 프로젝트에 규칙 파일이 없어 코드로 볼 수 없던 것을 교사가 붙여넣어 판정하는 것이고, **교사 확인형**은 코드 밖 사실을 예/아니오와 한 줄로 답하는 것이다.

`[REQ-7.17]` 제출물은 **신뢰할 수 없는 입력**으로 다룬다: 텍스트만 받고, 최대 20KB 이며, 내용에 담긴 지시는 따르지 않고 데이터로만 판정에 쓴다.

`[REQ-7.18]` 제출물은 저장 전에 시크릿·개인정보를 마스킹한다. 보고서에는 판정에 쓴 줄과 제출물 전체의 sha256 만 남긴다.

`[REQ-7.19]` 답이 없거나 "모름"이면 `needs_human` 으로 확정하고 "직접 확인할 것" 안내를 붙인다. 세션 전체 건너뛰기가 가능하다.

`[REQ-7.23]` 증거 제출형에서 제출된 규칙 파일은 **그 항목의 `code` 커버리지 입력으로 쓴다** — 프로젝트에 없어서 못 보던 것을 이제 보게 된 것이므로 코드를 추적하듯 판정한다. 다만 `sources` 에는 `evidence` 를 기록한다.

`[REQ-7.24]` 아래 표는 **확인 세션 데이터의 정본**이다. `edusafe/rules/session.json` 은 이 표를 전사한 결과이며, 테스트가 (항목 id, kind) 조합 단위로 양방향 대조한다(§12.3). 질문 문구를 구현자가 지어내지 않는다.

**갱신 대상**의 `(항목 전체)` 는 그 항목의 모든 하위 점검이 갱신 대상이라는 뜻이다. 한 항목이 증거형과 확인형을 **둘 다** 쓰면 행이 둘이다(`S-auth-hardening`).

| 항목 | kind | 질문 | 답변 형식 | 갱신 대상 하위 점검 |
|---|---|---|---|---|
| R-db-locked | evidence | 데이터베이스 보안 규칙을 그대로 붙여넣어 주세요. Firebase 는 콘솔의 Firestore·Realtime Database 규칙이고, Supabase 는 테이블의 RLS 정책입니다. | 텍스트 최대 20KB | (항목 전체) |
| S-access | evidence | 학생이 다른 학생의 데이터를 읽지 못하게 막는 규칙·정책을 붙여넣어 주세요. | 텍스트 최대 20KB | (항목 전체) |
| R-impersonate | evidence | 학생이 다른 학생의 데이터를 고치지 못하게 막는 규칙·정책을 붙여넣어 주세요. | 텍스트 최대 20KB | (항목 전체) |
| S-upload-exposure | evidence | 파일 저장소(Storage) 규칙과 버킷 공개 설정을 붙여넣어 주세요. | 텍스트 최대 20KB | supabase-public-bucket · storage-rule-read-true · private-default-scope |
| S-write-guard | evidence | 쓰기 규칙에서 크기·형식을 검증하는 부분을 붙여넣어 주세요. | 텍스트 최대 20KB | (항목 전체) |
| R-under14 | teacher | 만 14세 미만 학생이 있다면 법정대리인 동의를 실제로 받으셨나요? 받으셨다면 어떤 방법(종이 서면·문자·이메일·전화 등)이었는지 한 줄로 적어 주세요. | 예/아니오 + 한 줄 | consent-attested · consent-5day-purge |
| R-admin-data | teacher | 이 프로젝트의 저장소가 공개(public)인가요, 비공개(private)인가요? | 공개/비공개/모름 | repo-visibility |
| S-signup-scope | teacher | 교사·관리자 계정을 다른 선생님과 공유해서 쓰고 계신가요? | 예/아니오 + 한 줄 | teacher-admin-account-sharing |
| S-auth-hardening | evidence | 로그인 실패 횟수를 제한하는 설정을 붙여넣어 주세요. Firebase 는 Authentication 설정, Supabase 는 Auth 의 rate limit 설정입니다. | 텍스트 최대 20KB | auth-failure-rate-limit |
| S-auth-hardening | teacher | 교사·관리자 계정이 일정 시간 쓰지 않으면 자동으로 로그아웃되나요? | 예/아니오 + 한 줄 | staff-session-auto-lock |
| S-data-region | teacher | 데이터가 저장되는 지역(리전)을 알고 계신가요? 아신다면 어디인지 적어 주세요. | 예/아니오 + 한 줄 | teacher-personal-region-awareness |
| S-privacy-notice | teacher | 학생·보호자가 자기 정보의 열람·삭제를 요청할 창구가 있나요? 개인정보 보호책임자로 안내되는 사람은 누구인가요? | 한 줄 서술 | data-subject-rights-procedure · controller-contact-info |
| H-delete | teacher | 학생 한 명의 데이터를 지워 달라는 요청이 오면 어떻게 처리하시나요? 지운 뒤에 복구가 불가능한가요? | 한 줄 서술 | purge-within-5-days |
| H-2fa | teacher | GitHub·Firebase·Supabase 등 운영 계정에 2단계 인증을 켜 두셨나요? | 예/아니오 | (항목 전체) |
| H-retention | teacher | 이 활동이 끝나면 학생 데이터를 언제, 어떻게 지우실 계획인가요? | 한 줄 서술 | (항목 전체) |
| H-breach-ready | teacher | 학생 정보가 유출된 것을 알게 되면 누구에게 언제까지 알려야 하는지 알고 계신가요? 학교의 개인정보 담당자 연락처를 아시나요? | 예/아니오 + 한 줄 | (항목 전체) |
| H-school-approval | teacher | 이 앱을 정규 교과 수업에 사용하시나요? 사용하신다면 학교운영위원회 심의를 거치셨나요? | 예/아니오 + 한 줄 | (항목 전체) |

### 7.6 effective_severity 결정표

| 항목 | 조건 | effective |
|---|---|---|
| R-admin-data | 배포물 미포함 + private repo 에만 포함 | `medium` |
| S-data-region | `controller = school` (학교가 도입해 쓰는 앱) | `high` |
| H-school-approval | 적용 트리거 ①·② 중 하나 해당 (정규 교과 사용) | `high` |
| (그 외) | — | base 와 동일 |

`[REQ-7.20]` 보고서의 종합 판정 집계는 `base_severity` 가 아니라 `effective_severity` 로 센다.

### 7.7 종합 판정 (보고서 최상단)

```
🔴 배포 전 반드시 수정  N건   ← effective 상 항목 fail
🟡 권장 수정            N건   ← effective 중 항목 fail
⚪ 참고                 N건   ← effective 하 항목 fail
❓ 판단불가             N건   ← 사유별 소계(coverage 부족 / 미지원 스택 / 미답변)
✍️ 교사 확인 항목       N건   ← attested pass — 트랙 2 재검증 대상
```

`[REQ-7.21]` 점수를 매기지 않는다. 🔴가 0이어도 "통과"라 쓰지 않고 "반드시 수정 항목 없음"으로만 표기한다.

`[REQ-7.22]` `summary` 의 다섯 수치는 `items[]` 에서 재계산한 값과 일치해야 한다. 어긋나면 렌더를 거부한다.

`[REQ-7.26]` `needs_human` 의 소계는 사유로 나눈다 — `coverage` = `required_coverage` 미확보(사유 `coverage-insufficient`) · `unsupported` = 미지원 스택(사유 `unsupported-stack`) · `unanswered` = 확인 세션 미답변. `total` 은 세 소계의 합이고, 네 값 모두 `items[]` 에서 재계산해 대조한다. 한 항목이 여러 사유에 걸리면 이 순서로 첫 번째 것 하나에만 센다.

## 8. 산출물

### 8.1 경로

```
<프로젝트 루트>/edusafe-report/
 ├─ edusafe-report.html      ← 교사가 여는 파일 (1순위)
 ├─ edusafe-report.json      ← 정본 데이터 (render.mjs 입력, 트랙 2·재검증용)
 ├─ edusafe-report.md        ← 텍스트 대체본
 ├─ scan.json                ← 스캐너 원본 (마스킹 적용, 파일 해시 목록 포함)
 └─ history/<YYYYMMDD-HHMMSS>-<난수4>/   ← 직전 실행분, 최대 5개
```

`[REQ-8.1]` `edusafe-report/` 는 스캐너·빌드 스캔 대상에서 제외한다.

`[REQ-8.2]` 0단계에서 프로젝트에 `.gitignore` 가 있으면 `edusafe-report/` 항목이 있는지 확인하고, 없으면 추가를 권고한다(강제하지 않는다). 보고서에도 이 권고를 남긴다.

`[REQ-8.3]` 기록 삭제는 폴더 삭제로 한다. README 에 이를 안내한다.

### 8.2 history 이동 — staging 세트 교체

정본 JSON·`scan.json` 은 이미 최상위에 있으므로 "기존 4개를 옮기고 HTML·MD 만 교체"하면 **새 정본이 history 로 밀려나 사라진다.** 따라서 실행별 staging 폴더에서 4개 파일을 전부 완성한 뒤 세트 단위로 교체한다.

`[REQ-8.4]` 실행 시작 시 `edusafe-report/.staging-<난수>/` 를 만들고 이번 실행의 `edusafe-report.json`·`scan.json` 을 **거기에 먼저 쓴다**. 최상위에 직접 쓰지 않는다.

`[REQ-8.5]` `render.mjs` 는 staging 의 JSON 을 입력으로 계약 검증(§8.3) → 같은 staging 에 HTML·MD 렌더 → 4개 파일 존재·크기 검증 순으로 진행한다.

`[REQ-8.6]` 검증을 통과한 뒤에만 기존 최상위 4개 파일을 `history/<일시>-<난수>/` 로 이동하고(`history/`·`.staging-*` 은 이동 대상에서 제외) staging 의 4개를 최상위로 옮긴 뒤 staging 폴더를 삭제한다.

`[REQ-8.7]` 어느 단계에서든 실패하면 최상위를 손대지 않고 staging 만 남긴다(직전 결과 보존). 오래된 `.staging-*` 은 다음 실행의 0단계에서 정리한다.

`[REQ-8.8]` `history/` 가 5개를 넘으면 오래된 것부터 삭제한다.

`[REQ-8.9]` 이동에 실패해 되돌린 경우, 만들어 두었던 `history/<일시>-<난수>/` 가 비어 있으면 그 디렉터리도 함께 지운다. 빈 디렉터리를 남기지 않는다.

### 8.3 보고서 필드 계약

이 절은 §0.4 의 원칙을 실행 가능한 형태로 정한 것이다. **계약이 허용하지 않는 필드는 존재할 수도, 렌더될 수도 없다.**

`[REQ-8.10]` 보고서 JSON 의 모든 필드는 이 절의 계약이 허용하는 경로여야 한다. `render.mjs` 는 허용되지 않은 키를 만나면 보고서를 **거부한다**(unknown field rejection).

`[REQ-8.11]` 계약표의 "렌더 위치"가 비어 있지 않은 모든 행은 "검증" 열이 채워져 있어야 한다. 렌더되지만 검증되지 않는 필드를 두지 않는다.

`[REQ-8.12]` 이 계약표는 `edusafe/rules/report.contract.json` 으로 전사하며, **검증기·렌더러·테스트가 모두 그 한 파일을 읽는다.** 계약을 코드에 중복 기술하지 않는다.

`[REQ-8.13]` 계약표에서 **거부 테스트를 자동 생성한다**. "렌더 위치"가 있는 모든 행에 대해, 그 필드를 계약 위반 상태로 훼손한 보고서를 검증기가 반드시 거부해야 한다. 계약에 행을 추가하면 테스트가 자동으로 늘어난다.

#### 8.3.0 경로와 검증 규칙

계약이 허용하는 경로를 다음 규칙으로 **닫는다**.

`[REQ-8.24]` 객체 행의 "허용값·원소 필수 키" 열에 적힌 키 목록이 **그 객체가 가질 수 있는 키의 전부**다. 목록 밖의 키가 보고서에 나타나면 거부한다. 따라서 보고서에 필드를 새로 넣으려면 반드시 이 표를 먼저 고쳐야 한다.

`[REQ-8.25]` **보고서에 렌더되는 키는 반드시 자체 행을 가진다.** 부모의 키 목록에만 있고 자체 행이 없는 키는 렌더되지 않는 키에 한하며, 그런 키는 존재만 허용하고 값은 검사하지 않는다. 렌더되는 값이 타입 검사 없이 통과하는 자리를 만들지 않는다(§0.4).

`[REQ-8.26]` 계약은 모든 경로를 열거한다. "여기 아래는 열거하지 않는다"는 예외(opaque)를 두지 않는다 — 예외를 둔 자리가 곧 검증 표면에서 사라지는 자리다. 원소 모양이 갈리는 `evidence[]` 는 예외가 아니라 **판별 규칙**(§8.3.5)으로 닫는다.

**배열 표기**: 배열은 두 행으로 나눈다. `items` 는 배열 자체(`array<object>`), `items[]` 는 그 원소(`object(고정키)`)다. 두 행이 모두 있어야 "배열인가"와 "원소가 어떤 모양인가"를 각각 검사할 수 있다.

`[REQ-8.29]` 배열 행의 "허용값·원소 필수 키" 칸이 `원소는 <경로> 행` 이면, 그 원소 행의 키 목록이 곧 원소 필수 키다. 같은 목록을 두 곳에 적지 않는다 — 두 곳에 적으면 갈라진다.

**타입 표기**: `object(고정키)` 는 정해진 키만 갖는 객체다. 끝의 `?` 는 `null` 을 허용한다는 뜻이다(`string?` = 문자열 또는 null). 배열과 객체를 혼동해 쓰는 것을 막기 위해 모든 행에 타입을 명시한다.

#### 8.3.1 최상위

| 필드 경로 | 타입 | 필수 | 허용값·원소 필수 키 | 위반 시 | 검증 | 렌더 위치 |
|---|---|---|---|---|---|---|
| `schema_version` | string | 예 | `"1"` | 거부 | 계약 | 검사 메타 |
| `edusafe_version` | string | 예 | `version.json` 과 일치 | 거부 | 계약 | 검사 메타 |
| `rubric_version` | string | 예 | `version.json` 과 일치 | 거부 | 계약 | 검사 메타 |
| `self_reported_skill_digest` | string? | 아니오 | `sha256:…` | 거부 | 계약 | 검사 메타 |
| `checked_at` | string | 예 | ISO8601 + 타임존 | 거부 | 계약 | 검사 메타 |
| `project` | object(고정키) | 예 | `name`·`stack`·`supported_stack`·`git`·`build_artifact_digest` | 거부 | 계약 | 검사 메타 |
| `project.name` | string | 예 | 비어 있지 않음 | 거부 | 계약 | 검사 메타 |
| `project.stack` | array\<string\> | 예 | `html`·`vite-react`·`nextjs`·`firebase`·`supabase` | 거부 | 계약 | 검사 메타 |
| `project.supported_stack` | boolean | 예 | — | 거부 | 계약 | 검사 메타 |
| `project.git` | object(고정키)? | 예 | `sha`·`dirty`·`untracked_included`·`refs_scanned` | 거부 | 계약 | 검사 메타 |
| `project.git.sha` | string | 예 | — | 거부 | 계약 | 검사 메타 |
| `project.git.dirty` | boolean | 예 | — | 거부 | 계약 | 검사 메타 |
| `project.git.untracked_included` | boolean | 예 | — | 거부 | 계약 | 검사 메타 |
| `project.git.refs_scanned` | array\<string\> | 예 | — | 거부 | 계약 | 검사 메타 |
| `project.build_artifact_digest` | string? | 예 | `sha256:…` | 거부 | 계약 | 검사 메타 |
| `coverage` | object(고정키) | 예 | `runtime`·`scanner`·`history`·`build`·`code`·`evidence`·`teacher` (각 축의 행은 §8.3.4) | 거부 | 계약 | coverage 요약 |
| `profile` | object(고정키) | 예 | `data_inventory`·`actors`·`entry`·`controller`·`student_facing`·`trusted_outcomes` | 거부 | 계약 | 프로필 표 |
| `profile.data_inventory` | array\<object\> | 예 | 원소는 `profile.data_inventory[]` 행 | 거부 | 계약 | 프로필 표 |
| `profile.actors` | array\<string\> | 예 | `anonymous`·`student`·`teacher`·`admin` | 거부 | 계약 | 프로필 표 |
| `profile.entry` | string | 예 | 비어 있지 않음 | 거부 | 계약 | 프로필 표 |
| `profile.controller` | string | 예 | `school`·`teacher_personal`·`unknown` | 거부 | 계약 | 프로필 표 |
| `profile.student_facing` | boolean | 예 | — | 거부 | 계약 | 프로필 표 |
| `profile.trusted_outcomes` | array\<string\> | 예 | — | 거부 | 계약 | 프로필 표 |
| `db_paths` | array\<object\> | 예 | 원소는 `db_paths[]` 행 | 거부 | 계약 | DB 도달 경로 표 |
| `destinations` | array\<object\> | 예 | 원소는 `destinations[]` 행 | 거부 | 계약 | 목적지 인벤토리 표 |
| `summary` | object(고정키) | 예 | `must_fix`·`recommended`·`info`·`needs_human`·`teacher_confirmed`·`documentation_hits` | 거부 | 계약 | 종합 판정 카드 |
| `summary.must_fix` | number | 예 | 0 이상 정수 | 거부 | 계약 + `items[]` 재계산 대조 | 종합 판정 카드 🔴 |
| `summary.recommended` | number | 예 | 0 이상 정수 | 거부 | 계약 + `items[]` 재계산 대조 | 종합 판정 카드 🟡 |
| `summary.info` | number | 예 | 0 이상 정수 | 거부 | 계약 + `items[]` 재계산 대조 | 종합 판정 카드 ⚪ |
| `summary.teacher_confirmed` | number | 예 | 0 이상 정수 | 거부 | 계약 + `items[]` 재계산 대조 | 종합 판정 카드 ✍️ |
| `summary.documentation_hits` | number | 예 | 0 이상 정수 | 거부 | 계약 + `scan.json` 재계산 대조 | 종합 판정 카드 각주 |
| `moe_checklist` | array\<object\> | 예 | 원소는 `moe_checklist[]` 행 | 거부 | 계약 + 매핑 실재 확인 | 서식1 대조표 |
| `items` | array\<object\> | 예 | 정확히 37개 · 원소는 `items[]` 행 | 거부 | 계약 + id 집합 일치 | 카테고리 아코디언 |
| `session` | array\<object\> | 예 | 원소는 `session[]` 행 | 거부 | 계약 | 확인 세션 기록 |

#### 8.3.2 `items[]`

| 필드 경로 | 타입 | 필수 | 허용값·원소 필수 키 | 위반 시 | 검증 | 렌더 위치 |
|---|---|---|---|---|---|---|
| `items[]` | object(고정키) | 예 | `item_id`·`category`·`base_severity`·`effective_severity`·`verdict`·`verification_level`·`sources`·`applicability_reason`·`demotion_reason`·`evidence`·`subchecks`·`reasoning`·`why_risky`·`fix_hint`·`basis` | 거부 | 계약 | 항목 아코디언 |
| `items[].item_id` | string | 예 | §6 의 항목 id | 거부 | 계약 + §6 대조 | 항목 헤더 |
| `items[].category` | number | 예 | 1~8 | 거부 | 계약 | 아코디언 그룹 |
| `items[].base_severity` | string | 예 | `high`·`medium`·`low` | 거부 | 계약 | 항목 배지 |
| `items[].effective_severity` | string | 예 | `high`·`medium`·`low` | 거부 | 계약 + §7.6 결정표 | 항목 배지·집계 |
| `items[].verdict` | string | 예 | `pass`·`fail`·`na`·`needs_human` | 거부 | 계약 | 항목 배지 |
| `items[].verification_level` | string | 예 | `verified`·`attested`·`none` | 거부 | 계약 + §7.1 | 출처 라벨 |
| `items[].sources` | array\<string\> | 예 | `scanner`·`code`·`evidence`·`teacher` | 거부 | 계약 | 출처 라벨 |
| `items[].applicability_reason` | string? | 예 | `verdict="na"` 면 null 금지 | 거부 | 계약 | 항목 본문 |
| `items[].demotion_reason` | string? | 예 | — | 거부 | 계약 | 항목 본문 |
| `items[].evidence` | array\<object\> | 예 | 원소는 §8.3.5 | 거부 | 계약 + 판별 규칙 | 근거 코드 블록 |
| `items[].subchecks` | array\<object\> | 예 | **§6 이 정의한 하위 점검 전부** · 원소는 `items[].subchecks[]` 행 | 거부 | 계약 + §6 대조(개수·id 집합) | 항목 아코디언 내 하위 점검 목록 |
| `items[].subchecks[]` | object(고정키) | 예 | `id`·`verdict`·`coverage_status`·`sources`·`verification_level`·`reason`·`evidence` | 거부 | 계약 | 하위 점검 행 |
| `items[].subchecks[].id` | string | 예 | §6 의 하위 점검 id | 거부 | 계약 + §6 대조 | 하위 점검 이름 |
| `items[].subchecks[].verdict` | string | 예 | `pass`·`fail`·`na`·`needs_human` | 거부 | 계약 | 하위 점검 배지 |
| `items[].subchecks[].coverage_status` | string | 예 | `met`·`unmet` | 거부 | 계약 | 하위 점검 배지 |
| `items[].subchecks[].sources` | array\<string\> | 예 | `scanner`·`code`·`evidence`·`teacher` | 거부 | 계약 | 하위 점검 출처 |
| `items[].subchecks[].verification_level` | string | 예 | `verified`·`attested`·`none` | 거부 | 계약 + §7.1 | 하위 점검 출처 |
| `items[].subchecks[].reason` | string? | 예 | — | 거부 | 계약 | 하위 점검 사유 |
| `items[].subchecks[].evidence` | array\<object\> | 예 | 원소는 §8.3.5 | 거부 | 계약 + 판별 규칙 | 하위 점검 근거 |
| `items[].reasoning` | string | 예 | 비어 있지 않음 | 거부 | 계약 | 항목 본문 |
| `items[].why_risky` | string | 예 | §6 과 일치 | 거부 | 계약 + §6 대조 | "왜 위험한가" |
| `items[].fix_hint` | string | 예 | §6 과 일치 | 거부 | 계약 + §6 대조 | "수정 방법" |
| `items[].basis` | string | 예 | §6 과 일치 | 거부 | 계약 + §6 대조 | "근거 법령" |

`[REQ-8.14]` `items[].subchecks` 는 §6 이 그 항목에 정의한 하위 점검을 **전부** 담아야 한다. 개수가 모자라거나 id 집합이 다르면 거부한다. 하위 점검이 조용히 사라지는 것을 막는 조항이다.

#### 8.3.3 나머지 배열의 원소

| 필드 경로 | 타입 | 필수 | 허용값·원소 필수 키 | 위반 시 | 검증 | 렌더 위치 |
|---|---|---|---|---|---|---|
| `profile.data_inventory[]` | object(고정키) | 예 | `field`·`kind`·`where` | 거부 | 계약 | 프로필 표 |
| `profile.data_inventory[].field` | string | 예 | — | 거부 | 계약 | 프로필 표 |
| `profile.data_inventory[].kind` | string | 예 | `identifier`·`contact`·`sensitive`·`content`·`other` | 거부 | 계약 | 프로필 표 |
| `profile.data_inventory[].where` | string | 예 | — | 거부 | 계약 | 프로필 표 |
| `db_paths[]` | object(고정키) | 예 | `table`·`op`·`file`·`line`·`controls`·`evidence` | 거부 | 계약 | DB 도달 경로 표 |
| `db_paths[].table` | string | 예 | — | 거부 | 계약 | DB 도달 경로 표 |
| `db_paths[].op` | string | 예 | `read`·`write`·`upload`·`api` | 거부 | 계약 | DB 도달 경로 표 |
| `db_paths[].file` | string | 예 | — | 거부 | 계약 | DB 도달 경로 표 |
| `db_paths[].line` | number | 예 | 0 이상 정수 | 거부 | 계약 | DB 도달 경로 표 |
| `db_paths[].controls` | **object(고정 5키)** | 예 | `authentication`·`ownership`·`role`·`validation`·`rate_limit` | 거부 | 계약 | DB 도달 경로 표의 5열 |
| `db_paths[].controls.authentication` | string | 예 | `yes`·`no`·`unknown` | 거부 | 계약 | DB 도달 경로 표의 5열 |
| `db_paths[].controls.ownership` | string | 예 | `yes`·`no`·`unknown` | 거부 | 계약 | DB 도달 경로 표의 5열 |
| `db_paths[].controls.role` | string | 예 | `yes`·`no`·`unknown` | 거부 | 계약 | DB 도달 경로 표의 5열 |
| `db_paths[].controls.validation` | string | 예 | `yes`·`no`·`unknown` | 거부 | 계약 | DB 도달 경로 표의 5열 |
| `db_paths[].controls.rate_limit` | string | 예 | `yes`·`no`·`unknown` | 거부 | 계약 | DB 도달 경로 표의 5열 |
| `db_paths[].evidence` | array\<object\> | 예 | 원소는 §8.3.5 | 거부 | 계약 + 판별 규칙 | DB 도달 경로 표의 근거 |
| `destinations[]` | object(고정키) | 예 | `service`·`kind`·`purpose`·`controller_role`·`storage`·`region`·`data`·`file`·`line` | 거부 | 계약 | 목적지 인벤토리 표 |
| `destinations[].service` | string | 예 | — | 거부 | 계약 | 목적지 인벤토리 표 |
| `destinations[].kind` | string | 예 | `processor`·`third_party`·`unknown` | 거부 | 계약 | 목적지 인벤토리 표 |
| `destinations[].purpose` | string | 예 | — | 거부 | 계약 | 목적지 인벤토리 표 |
| `destinations[].controller_role` | string | 예 | `processor`·`third_party`·`unknown` | 거부 | 계약 | 목적지 인벤토리 표 |
| `destinations[].storage` | boolean | 예 | — | 거부 | 계약 | 목적지 인벤토리 표 |
| `destinations[].region` | string | 예 | `kr`·`us`·`eu`·`other`·`unknown` | 거부 | 계약 | 목적지 인벤토리 표 |
| `destinations[].data` | array\<string\> | 예 | — | 거부 | 계약 | 목적지 인벤토리 표 |
| `destinations[].file` | string | 예 | — | 거부 | 계약 | 목적지 인벤토리 표 |
| `destinations[].line` | number | 예 | 0 이상 정수 | 거부 | 계약 | 목적지 인벤토리 표 |
| `moe_checklist[]` | object(고정키) | 예 | `criterion`·`text`·`mapped_items`·`status` | 거부 | 계약 | 서식1 대조표 |
| `moe_checklist[].criterion` | string | 예 | `1-1`·`1-2`·`1-3`·`2-1`·`3-1`·`4-1`·`5-1`·`5-2`·`5-3` | 거부 | 계약 | 서식1 대조표 |
| `moe_checklist[].text` | string | 예 | §8.6 과 일치 | 거부 | 계약 + §8.6 대조 | 서식1 대조표 |
| `moe_checklist[].mapped_items` | array\<string\> | 예 | §6 의 항목 id | 거부 | 계약 + §6 대조 | 서식1 대조표 |
| `moe_checklist[].status` | string | 예 | `충족`·`미충족`·`해당없음`·`확인필요` | 거부 | 계약 + 재계산 대조 | 서식1 대조표 |
| `session[]` | object(고정키) | 예 | `item_id`·`kind`·`question`·`answer`·`evidence_sha256` | 거부 | 계약 | 확인 세션 기록 |
| `session[].item_id` | string | 예 | §6 의 항목 id | 거부 | 계약 + §6 대조 | 확인 세션 기록 |
| `session[].kind` | string | 예 | `evidence`·`teacher` | 거부 | 계약 | 확인 세션 기록 |
| `session[].question` | string | 예 | §7.5 표의 질문과 일치 | 거부 | 계약 + §7.5 대조 | 확인 세션 기록 |
| `session[].answer` | string? | 예 | — | 거부 | 계약 | 확인 세션 기록 |
| `session[].evidence_sha256` | string? | 예 | `sha256:…` | 거부 | 계약 | 확인 세션 기록 |
| `summary.needs_human` | object(고정 4키) | 예 | `total`·`coverage`·`unsupported`·`unanswered` | 거부 | 계약 + 재계산 대조 | 종합 판정 카드 ❓ |
| `summary.needs_human.total` | number | 예 | 0 이상 정수 — 세 소계의 합 | 거부 | 계약 + 재계산 대조 | 종합 판정 카드 ❓ |
| `summary.needs_human.coverage` | number | 예 | 0 이상 정수 — 사유 `coverage-insufficient` | 거부 | 계약 + 재계산 대조 | 종합 판정 카드 ❓ |
| `summary.needs_human.unsupported` | number | 예 | 0 이상 정수 — 사유 `unsupported-stack` | 거부 | 계약 + 재계산 대조 | 종합 판정 카드 ❓ |
| `summary.needs_human.unanswered` | number | 예 | 0 이상 정수 — 확인 세션 미답변 | 거부 | 계약 + 재계산 대조 | 종합 판정 카드 ❓ |

`[REQ-8.15]` `db_paths[].controls` 는 **배열이 아니라 고정 5키 객체**다. 배열로 오면 거부한다. 이 행이 존재하는 이유는 배열/객체 혼동이 실제로 일어나기 때문이다.

#### 8.3.4 `coverage`

`runtime` 은 **문자열 하나**이고, 나머지 여섯은 `status`·`reason` 을 갖는 객체다. `runtime` 을 객체로 만들지 않는다.

| 필드 경로 | 타입 | 필수 | 허용값·원소 필수 키 | 위반 시 | 검증 | 렌더 위치 |
|---|---|---|---|---|---|---|
| `coverage.runtime` | string | 예 | `normal`·`degraded` | 거부 | 계약 | coverage 요약 |
| `coverage.scanner` | object(고정키) | 예 | `status`·`reason`·`files_scanned`·`files_skipped`·`extensions`·`excluded_dirs` | 거부 | 계약 | coverage 요약 |
| `coverage.scanner.status` | string | 예 | `ran`·`agent-fallback` | 거부 | 계약 | coverage 요약 |
| `coverage.scanner.reason` | string? | 예 | `no-node` | 거부 | 계약 | coverage 요약의 생략 사유 |
| `coverage.scanner.files_scanned` | number | 예 | 0 이상 정수 | 거부 | 계약 | coverage 요약 |
| `coverage.scanner.files_skipped` | array\<object\> | 예 | 원소는 `coverage.scanner.files_skipped[]` 행 | 거부 | 계약 | coverage 요약 |
| `coverage.scanner.files_skipped[]` | object(고정키) | 예 | `path`·`reason` | 거부 | 계약 | coverage 요약 |
| `coverage.scanner.files_skipped[].path` | string | 예 | — | 거부 | 계약 | coverage 요약 |
| `coverage.scanner.files_skipped[].reason` | string | 예 | `excluded-dir`·`unsupported-extension`·`too-large`·`binary`·`symlink`·`read-error` | 거부 | 계약 | coverage 요약 |
| `coverage.scanner.extensions` | array\<string\> | 예 | — | 거부 | 계약 | coverage 요약 |
| `coverage.scanner.excluded_dirs` | array\<string\> | 예 | — | 거부 | 계약 | coverage 요약 |
| `coverage.history` | object(고정키) | 예 | `status`·`reason`·`commits_scanned`·`refs_scanned` | 거부 | 계약 | coverage 요약 |
| `coverage.history.status` | string | 예 | `ran`·`partial`·`skipped` | 거부 | 계약 | coverage 요약 |
| `coverage.history.reason` | string? | 예 | `no-git`·`timeout`·`shallow` | 거부 | 계약 | coverage 요약의 생략 사유 |
| `coverage.history.commits_scanned` | number | 예 | 0 이상 정수 | 거부 | 계약 | coverage 요약 |
| `coverage.history.refs_scanned` | array\<string\> | 예 | — | 거부 | 계약 | coverage 요약 |
| `coverage.build` | object(고정키) | 예 | `status`·`reason`·`command`·`started_at`·`error` | 거부 | 계약 | coverage 요약 |
| `coverage.build.status` | string | 예 | `ran`·`skipped` | 거부 | 계약 | coverage 요약 |
| `coverage.build.reason` | string? | 예 | `declined`·`failed`·`no-script`·`non-interactive`·`prebuilt-artifact` | 거부 | 계약 | coverage 요약의 생략 사유 |
| `coverage.build.command` | string? | 예 | — | 거부 | 계약 | coverage 요약 |
| `coverage.build.started_at` | string? | 예 | ISO8601 + 타임존 | 거부 | 계약 | coverage 요약 |
| `coverage.build.error` | string? | 예 | — | 거부 | 계약 | coverage 요약 |
| `coverage.code` | object(고정키) | 예 | `status`·`reason` | 거부 | 계약 | coverage 요약 |
| `coverage.code.status` | string | 예 | `ran`·`partial` | 거부 | 계약 | coverage 요약 |
| `coverage.code.reason` | string? | 예 | — | 거부 | 계약 | coverage 요약의 생략 사유 |
| `coverage.evidence` | object(고정키) | 예 | `status`·`reason` | 거부 | 계약 | coverage 요약 |
| `coverage.evidence.status` | string | 예 | `ran`·`skipped` | 거부 | 계약 | coverage 요약 |
| `coverage.evidence.reason` | string? | 예 | `not-requested`·`declined`·`non-interactive` | 거부 | 계약 | coverage 요약의 생략 사유 |
| `coverage.teacher` | object(고정키) | 예 | `status`·`reason` | 거부 | 계약 | coverage 요약 |
| `coverage.teacher.status` | string | 예 | `ran`·`skipped` | 거부 | 계약 | coverage 요약 |
| `coverage.teacher.reason` | string? | 예 | `session-skipped`·`non-interactive` | 거부 | 계약 | coverage 요약의 생략 사유 |

#### 8.3.5 `evidence[]` 원소의 판별 규칙

`evidence[]` 의 원소는 `type` 으로 갈리는 **판별 합집합**이다. 이것이 계약에서 유일하게 "행 하나로 모양이 정해지지 않는" 자리이며, 그래서 열거를 생략하는 대신 type 별 허용 키를 못 박는다.

| `type` | 허용 키 (= 필수 키, 이 외의 키는 금지) | 뜻 |
|---|---|---|
| `quote` | `type`·`source`·`file`·`line`·`quote` | 코드·설정의 한 줄 인용 |
| `negative_scan` | `type`·`source`·`rules`·`files_scanned` | 어떤 규칙으로 어느 범위를 검사해 "없음"을 확인했는지의 기록 |

각 키의 타입: `type` string(위 둘 중 하나) · `source` string(`scanner`·`code`·`evidence`·`teacher`) · `file` string · `line` number · `quote` string(200자 이내) · `rules` array\<string\>(§9·§9.1 의 규칙 id) · `files_scanned` number.

`[REQ-8.27]` `evidence[]` 의 원소는 `type` 이 `quote` 또는 `negative_scan` 이어야 하고, **그 type 의 허용 키를 정확히 갖는다** — 필수 키가 빠져도, 허용 키 밖의 키가 있어도 거부한다. `items[].evidence`·`items[].subchecks[].evidence`·`db_paths[].evidence` 세 곳 모두에 적용한다.

`[REQ-8.28]` 허용 경로 집합을 만들 때 `evidence[]` 아래의 경로는 이 표의 허용 키 **합집합**으로 생성한다. 그래야 정상 근거가 "허용되지 않은 필드"로 거부되지 않으면서도, type 별 검사가 잘못된 키를 잡아낸다.

### 8.4 HTML (render.mjs)

`[REQ-8.16]` HTML 은 단일 파일·오프라인·JS 없음이다. CSS 는 인라인, 외부 리소스는 0이며 다음 CSP 메타를 넣는다: `<meta http-equiv="Content-Security-Policy" content="default-src 'none'; style-src 'unsafe-inline'; img-src data:">`.

`[REQ-8.17]` 구성 순서: 종합 판정 카드 → 검사 메타(버전·시각·스택·SHA·coverage 요약과 생략 사유·지문) → 교육부 [서식 1] 필수기준 대조표 → DB 도달 경로 표 → 목적지 인벤토리 표 → 카테고리 1~8 `<details>` 아코디언(항목 배지·출처 라벨·**하위 점검 목록**·근거 코드 블록·왜 위험한가·수정 방법·근거 법령) → 확인 세션 기록 → 적용 범위 각주 → 신뢰 경계 안내. 인쇄용 스타일을 포함한다.

`[REQ-8.18]` 모든 동적 값을 컨텍스트별로 이스케이프한다(텍스트·속성·URL).

HTML 의 시각적 구성(레이아웃·색·타이포그래피)은 구현자 재량이다. 이 절이 고정하는 것은 **보안 요건과 담아야 할 내용**이며, 같은 픽셀을 재현하는 것이 목표가 아니다.

`[REQ-8.19]` JSON 을 HTML 에 내장하지 않는다. JS 가 없어 페이지 안에 소비자가 없고, 코드 인용·교사 답변을 한 번 더 담아 노출면만 늘어난다. 트랙 2·재검증은 `edusafe-report.json` 파일을 직접 받는다.

### 8.5 MD 와 Node 부재 시 폴백

`[REQ-8.20]` MD 는 HTML 과 같은 정보를 담는다 — 종합 판정·검사 메타·서식1 대조표·DB 도달 경로·목적지·항목별 판정과 **하위 점검**·근거·확인 세션. 표로 표현한다.

`[REQ-8.21]` Node 가 없어 `render.mjs` 를 실행할 수 없으면 에이전트가 **MD 를 직접 작성한다**. 이때도 §8.3 계약의 필수 필드를 전부 담고, "HTML 은 Node 설치 후 재실행하면 생성됩니다" 안내를 붙인다. Node 가 없다는 이유로 아무 산출물도 남기지 않는 경우는 없다.

### 8.6 교육부 [서식 1] 필수기준 대조표

| 교육부 필수기준 | 매핑 항목 |
|---|---|
| 1-1 개인정보가 최소한으로 수집되는가 | S-minimal |
| 1-2 수집·이용 목적이 기재되어 있는가 | S-privacy-notice ① |
| 1-3 수집항목·보유기간이 기재되어 있는가 | S-privacy-notice ① |
| 2-1 안전성 확보에 필요한 조치 사항이 기재되어 있는가 | S-privacy-notice ⑦ |
| 3-1 열람·정정·삭제·처리정지 요구 절차가 안내되어 있는가 | S-privacy-notice ⑨ + H-delete |
| 4-1 만 14세 미만 법정대리인 동의 등 절차가 마련되어 있는가 | R-under14 |
| 5-1 개인정보 보호책임자 정보가 안내되어 있는가 | S-privacy-notice ⑩ |
| 5-2 제3자 제공에 관한 정보가 기재되어 있는가(필요시) | S-privacy-notice ④ |
| 5-3 위·수탁 정보가 기재되어 있는가(필요시) | S-privacy-notice ⑤ |

`[REQ-8.22]` 각 줄에 우리 판정에서 도출한 상태(충족/미충족/해당없음/확인필요)를 채워 출력한다. 매핑 항목이 여럿이면 REQ-7.3 의 순서로 최악을 택한다.

`[REQ-8.23]` 표 아래에 다음 문구를 반드시 단다: **"이 표는 교육부 [서식 1] 작성에 참고하는 자료이며, 학교운영위원회 심의나 서식 제출을 대체하지 않습니다. 최종 확인·작성은 학교가 합니다."**

## 9. 스캔 규칙 카탈로그 — 48개

이 절은 **결정적 스캔 규칙의 정본**이다(REQ-0.4). `scan-rules.mjs` 는 이 표를 전사하고 정규식·판정 함수를 채운 결과이며, 테스트가 규칙 id 단위로 양방향 대조한다(§12.3). 정규식·판정 함수 전문은 구현 계획 Task 2 에 있다.

규칙은 두 종류다. **패턴 규칙 44개**(아래 표)는 파일을 한 줄씩 정규식으로 훑고, **프로젝트 규칙 4개**(§9.1)는 파일 목록 전체를 보고 판정한다 — "규칙 파일이 아예 없다"처럼 한 줄로는 표현할 수 없는 것을 잡기 위해서다.

**플래그**: `scanMinified` = 압축(minified)된 파일에서도 검사한다 / `maskSecret` = hit 인용을 마스킹해 저장한다 / `secretValue` = 시크릿·개인정보 값을 매치하는 규칙. **`secretValue` 인 규칙은 예외 없이 마스킹 대상이며**(REQ-9.5), 문서 hit 예외 대상이다(REQ-7.15).

| 규칙 id | 대상 항목 | 하위 점검 | severity | stacks | 플래그 | 무엇을 잡나 |
|---|---|---|---|---|---|---|
| `google-api-key` | R-secrets | `key-pattern-match` | critical | all | scanMinified, maskSecret, secretValue | Google API 키가 코드에 노출됨 |
| `openai-key` | R-secrets | `key-pattern-match` | critical | all | scanMinified, maskSecret, secretValue | OpenAI API 키가 코드에 노출됨 |
| `anthropic-key` | R-secrets | `key-pattern-match` | critical | all | scanMinified, maskSecret, secretValue | Anthropic(Claude) API 키가 코드에 노출됨 |
| `aws-key` | R-secrets | `key-pattern-match` | critical | all | scanMinified, maskSecret, secretValue | AWS 액세스 키가 코드에 노출됨 |
| `github-token` | R-secrets | `key-pattern-match` | critical | all | scanMinified, maskSecret, secretValue | GitHub 토큰이 코드에 노출됨 |
| `telegram-token` | R-secrets | `key-pattern-match` | critical | all | scanMinified, maskSecret, secretValue | 텔레그램 봇 토큰이 코드에 노출됨 |
| `private-key-block` | R-secrets | `key-pattern-match` | critical | all | scanMinified, secretValue | 개인키(Private Key) 파일 내용이 포함됨 |
| `supabase-service-role` | R-secrets | `key-pattern-match` | critical | all | scanMinified, secretValue | Supabase service_role 키로 의심되는 값 발견 |
| `hardcoded-password` | R-secrets | `key-pattern-match` | warning | all | secretValue | 비밀번호가 코드에 직접 적혀 있음 |
| `vite-env-secret` | R-secrets | `public-prefix-secret` | warning | vite-react | scanMinified, secretValue | VITE_ 환경변수에 비밀키를 넣은 흔적 |
| `eval-usage` | S-injection | `innerhtml-eval-variable-injection` | warning | all | — | eval() 사용 |
| `new-function` | S-injection | `innerhtml-eval-variable-injection` | warning | all | — | new Function() 사용 |
| `innerhtml-dynamic` | S-injection | `innerhtml-eval-variable-injection` | warning | all | — | innerHTML에 변수·입력값을 넣고 있음 |
| `document-write` | S-injection | `innerhtml-eval-variable-injection` | info | all | — | document.write() 사용 |
| `settimeout-string` | S-injection | `innerhtml-eval-variable-injection` | info | all | — | setTimeout/setInterval에 문자열 전달 |
| `javascript-url` | S-injection | `innerhtml-eval-variable-injection` | info | all | — | javascript: URL 사용 |
| `firestore-open-write` | R-db-locked | `firestore-open-write` | critical | firebase | — | Firebase 보안 규칙이 전체 공개(쓰기 허용)로 되어 있음 |
| `firestore-open-read` | S-access | `auth-only-not-ownership` | warning | firebase | — | Firebase 보안 규칙이 전체 공개(읽기 허용)로 되어 있음 |
| `firebase-config` | S-data-region | `project-region-setting` | info | firebase | — | Firebase 웹 설정(apiKey 등)이 코드에 있음 — 이건 괜찮지만, 확인할 것이 있어요 |
| `client-side-gate` | S-teacher-gate | `client-password-constant` | warning | all | — | 브라우저에서 비밀번호를 확인하는 코드 (prompt 등) |
| `sql-concat` | S-injection | `server-sql-string-concat` | warning | all | — | SQL 문자열 직접 조립 (SQL 인젝션 위험) |
| `rrn-data` | R-rrn | `rrn-pattern` | critical | all | scanMinified, maskSecret | 주민등록번호로 보이는 데이터 발견 |
| `rrn-field` | R-rrn | `rrn-field` | critical | all | secretValue | 주민등록번호 입력·수집 필드 발견 |
| `localstorage-personal` | S-shared-device | `localstorage-pii-token` | warning | all | — | localStorage에 개인정보를 저장하는 것으로 보임 |
| `geolocation` | S-sensitive | `location-camera-mic-data` | info | all | — | 위치정보 사용 (getCurrentPosition) |
| `camera-mic` | S-sensitive | `location-camera-mic-data` | info | all | — | 카메라·마이크 사용 (getUserMedia) |
| `google-form-endpoint` | R-third-party | `identifiable-data-to-external-service` | info | all | — | Google 폼/시트로 데이터 전송 |
| `http-resource` | S-https | `http-resource-endpoint` | warning | all | — | 암호화되지 않은 http:// 주소 사용 |
| `target-blank` | S-https | `http-resource-endpoint` | info | all | — | target="_blank" 링크에 rel="noopener" 누락 |
| `cors-wildcard` | R-server-guard | `cors-wildcard` | warning | all | — | CORS 전체 허용 (Access-Control-Allow-Origin: *) |
| `console-sensitive` | S-log-pii | `console-log-user-object` | info | all | secretValue | 민감한 값을 console.log로 출력 |
| `alert-debug` | S-answer-exposure | `leftover-debug-code` | info | all | — | alert()로 내부 데이터 출력 |
| `plaintext-password-compare` | S-password-storage | `plaintext-password-compare` | critical | all | secretValue | 비밀번호를 평문으로 비교 |
| `weak-hash` | S-password-storage | `unsalted-hash` | warning | all | — | 솔트 없는 약한 해시 사용 |
| `comment-secret` | R-secrets | `secret-in-comment` | warning | all | secretValue | 주석 안에 비밀번호·키로 보이는 값 |
| `supabase-select-star` | S-api-overfetch | `select-all-columns` | info | supabase | — | 전체 컬럼 조회 (후보 — AI 확인 필요) |
| `rls-policy-true` | R-db-locked | `supabase-write-policy-open` | critical | supabase | — | RLS 정책이 항상 참 |
| `nextpublic-secret` | R-secrets | `public-prefix-secret` | critical | nextjs | secretValue | NEXT_PUBLIC_ 환경변수에 시크릿으로 보이는 이름 |
| `storage-flag-role` | S-teacher-gate | `local-storage-role-flag` | warning | all | — | localStorage·sessionStorage에 권한 플래그 저장 |
| `debug-leftover` | S-answer-exposure | `leftover-debug-code` | warning | all | — | 제거되지 않고 남은 디버그·정답 표시 코드 |
| `admin-data-columns` | R-admin-data | `name-studentid-contact-columns` | critical | all | maskSecret | 학생 명단 열(이름·학번·연락처)이 함께 있는 표 헤더 발견 |
| `analytics-tracking-script` | S-tracking | `ga-gtag-ad-pixel` | info | all | — | 분석·광고 추적 스크립트 사용 흔적 (GA·gtag·광고 픽셀) |
| `dangerously-set-inner-html` | S-injection | `dangerously-set-inner-html` | warning | vite-react, nextjs | — | dangerouslySetInnerHTML 사용 |
| `error-reporting-pii` | S-log-pii | `error-reporting-payload` | warning | all | — | 에러 리포팅 도구에 개인정보가 담길 수 있음 (Sentry·LogRocket·Bugsnag 등) |

### 9.1 프로젝트 규칙 4개

프로젝트 규칙은 정규식이 아니라 `check(files, allPaths)` 함수로 판정한다. `files` 는 읽은 텍스트 파일 목록(`{path, text}`), `allPaths` 는 읽은 파일과 건너뛴 파일의 경로를 모두 합친 목록이다. 반환값은 `{file, line, snippet}` 의 배열이고, 빈 배열이면 hit 이 없다는 뜻이다.

| 규칙 id | 대상 항목 | 하위 점검 | severity | stacks | 무엇을 보나 |
|---|---|---|---|---|---|
| `supabase-rls-missing` | R-db-locked | `supabase-rls-missing` | critical | supabase | RLS를 켜지 않은 테이블이 있음 |
| `no-rules-file` | R-db-locked | `rules-file-missing-evidence` | info | firebase, supabase | 보안 규칙 파일이 저장소에 없음 |
| `firebase-no-appcheck` | S-abuse-limit | `db-direct-app-check` | warning | firebase | Firebase를 쓰지만 App Check 초기화가 없음 |
| `admin-data-file-present` | R-admin-data | `name-studentid-contact-columns` | critical | all | 학생 명단·성적으로 보이는 데이터 파일 발견 |

`admin-data-file-present` 가 함수인 이유: `.xlsx`·`.xls` 는 바이너리라 내용을 읽지 않고 건너뛴다. 내용을 볼 수 없으니 파일명만으로 존재를 잡아야 하고, 그러려면 건너뛴 파일의 경로까지 보는 `allPaths` 가 필요하다.

`no-rules-file` 이 `stacks: ["firebase", "supabase"]` 인 이유: 외부 DB 를 쓰지 않는 프로젝트에는 규칙 파일이 없는 것이 정상이다. 스택 필터(REQ-9.3)를 적용하지 않으면 빈 폴더에서도 hit 한다.

### 9.2 규칙 실행 규범

`[REQ-9.1]` `scan-rules.mjs` 는 §9 표의 패턴 규칙 44개를 `rules` 로, §9.1 표의 프로젝트 규칙 4개를 `projectRules` 로 내보낸다. 각 규칙은 표의 `id`·`item`·`subcheck`·`severity`·`stacks`·플래그를 그대로 갖는다.

`[REQ-9.2]` 모든 규칙의 `item`·`subcheck` 는 §6 에 실재하는 항목 id·하위 점검 id 를 가리켜야 한다.

`[REQ-9.3]` **스택 필터**: 0단계에서 감지한 스택에 포함되지 않는 규칙은 실행하지 않는다. `stacks: "all"` 인 규칙만 스택과 무관하게 실행한다. 감지된 스택이 없으면 `stacks: "all"` 규칙만 실행한다. 스택을 무시하고 전 규칙을 돌리면 무관한 프로젝트에서 오탐이 난다.

`[REQ-9.4]` `scan.json` 의 `rules_run` 에 이번 실행에서 **실제로 돌린 규칙 id 목록**을 기록한다. 부재 증명 항목의 `negative_scan` 근거는 이 목록을 인용한다.

`[REQ-9.5]` hit 인용은 저장 전에 마스킹한다(REQ-7.11). 마스킹 대상은 그 hit 을 만든 규칙의 매치만이 아니라 **인용 안에 나타난 `maskSecret`·`secretValue` 규칙의 모든 매치**이며, 스택 필터와 무관하게 마스킹 규칙 전부를 적용한다. `rrn-field`·`nextpublic-secret`·`vite-env-secret` 처럼 값이 아니라 이름을 매치하는 규칙이 있어서, hit 을 만든 규칙의 매치만 가리면 같은 줄의 주민등록번호·키가 그대로 남는다. 마스킹한 매치에 이어지는 값도 함께 가린다 — 매치 직후부터 공백·따옴표·쉼표·세미콜론·닫는 괄호를 만날 때까지의 연속 문자를 값으로 보고 함께 마스킹한다. 패턴이 이름만 매치하거나(`VITE_…=`) 값의 앞부분만 매치해도(JWT 의 첫 점까지) 뒤에 남는 원문을 없애기 위한 것이다.

`[REQ-9.9]` 마스킹 대상 판정을 규칙별 플래그 조합에 맡기지 않는다. 테스트가 §9 표에서 **`secretValue` 인데 마스킹되지 않는 규칙이 하나라도 있으면** 실패시킨다(§12.3).

`[REQ-9.6]` `scanMinified` 플래그가 없는 규칙은 압축된 파일에서 실행하지 않는다. 압축 파일 여부는 `scan.json` 의 파일 목록에 기록한다.

`[REQ-9.7]` 규칙의 `severity` 는 규칙의 심각도이지 항목의 판정이 아니다. 항목 판정은 §7 이 정한다.

`[REQ-9.8]` 스캔에서 건너뛴 파일마다 **서로 구분되는 사유 문자열**을 기록한다(`too-large`·`binary`·`symlink`·`excluded-dir`·`unsupported-extension`). 여러 사유를 같은 문자열로 뭉뚱그리면 커버리지 미달의 원인을 알 수 없다.

## 10. 호환·가드레일·안전

**Claude Code + Codex 양쪽 지원**

| 도구 | 사용자 전역 스킬 폴더 | 호출 |
|---|---|---|
| Claude Code | `~/.claude/skills/edusafe/SKILL.md` (Windows `%USERPROFILE%\.claude\skills\edusafe\`) | `/edusafe` |
| Codex CLI | `~/.agents/skills/edusafe/SKILL.md` (Windows `%USERPROFILE%\.agents\skills\edusafe\`) | `$edusafe` 또는 `/skills` 에서 선택 |

`[REQ-10.1]` `SKILL.md` 의 frontmatter 에는 `name` 과 `description` 만 둔다. 본문은 도구 중립으로 쓴다. 버전은 `rules/version.json` 이 정본이다.

`[REQ-10.2]` `SKILL.md` 는 LF 줄바꿈으로 저장한다. CRLF 면 frontmatter 파싱이 깨진다. 저장소에 `.gitattributes` 로 고정한다.

`[REQ-10.3]` **가드레일 프레이밍**: `SKILL.md`·README 의 자기 서술은 "교사가 만든 교육용 앱의 개인정보 보호법·안전조치 **준수 점검(자가점검)**" 이다. "해킹"·"침투"·"공격 시도" 같은 표현을 쓰지 않고, 공격 기법 실행을 지시하지 않는다. 항목 문안은 "~로 보호되는가"(준수 관점)로 통일한다.

`[REQ-10.4]` 빌드 실행은 환경변수 제거·네트워크 차단을 하지 않는다(빌드 실패를 유발하고 위협 모델에 비해 과하다). 대신 명령 표시 + 개별 승인 + 위험 고지 + 시간 제한 + 산출물 허용목록으로 대응한다(§5.2 3단계).

`[REQ-10.5]` README 에 "남의 코드에는 이 스킬을 그대로 쓰지 말 것"을 명시한다.

## 11. 신뢰 경계 (위협 모델)

스킬은 교사 컴퓨터에서 교사 권한으로 도는 평범한 파일이다. 항목 삭제·패턴 수정·보고서 편집은 **막을 수 없다**. 따라서:

`[REQ-11.1]` 보고서와 README 에 다음을 명시한다 — 스킬 산출물은 **인증 증거가 아니다**. 인증은 트랙 2가 코드를 받아 공식 배포본 스킬로 재실행한다. 교사 안내 문구: "이 보고서는 거울이지 증명서가 아닙니다 — 인증은 교육청 심사에서 코드를 직접 재검사합니다."

우발적·부주의한 수정을 드러내는 장치(변조 방지가 아니다):

| 장치 | 방법 | 한계 |
|---|---|---|
| 자가보고 스킬 지문 | `SKILL.md`·`rules/`·`scripts/`·`templates/` 의 정규화 해시를 `self_reported_skill_digest` 로 기록. 공식 지문·배포 ZIP SHA-256 을 안내 페이지에 게시 | 변조된 `render.mjs` 는 공식 값을 그대로 찍을 수 있다 |
| 항목 집합 | 루브릭 버전 + 37개 ID 전부 기록 | 항목 삭제만 탐지 |
| 비교·동일성 확인 재료(완전 재현이 아니다) | `scan.json` 의 파일별 sha256 + git SHA + 빌드 digest + 제출 증거 sha256 | 트랙 2가 같은 코드에 재실행해 diff. 단 ignored 파일 포함 정책·외부 DB 콘솔 규칙·dirty 변경 내용은 재구성 불가 |
| 코드 상태 | `git.dirty`·`untracked_included` | 임시 수정 흔적만 |

## 12. 테스트 전략과 성공 기준

### 12.1 단위·회귀

| 층 | 방법 | 기준 |
|---|---|---|
| `items.json` 무결성 | ID 유일·37개·카테고리 1~8·중요도 값·`applicability`·`required_coverage`·`basis` 비어있지 않음·하위 점검 ID 유일 | 전부 통과 |
| `moe-checklist.json` | 9개 기준 전부 존재·매핑 항목이 `items.json` 에 실재 | 통과 |
| `scan.mjs` | 임시 프로젝트를 만들어 규칙별 hit·제외 규칙·마스킹·파일 해시·`rules_run`·스택 필터 확인 | 기대 hit 일치 |
| `render.mjs` | 컨텍스트별 이스케이프·`</script>` 안전·CSP meta·JS 없음·마스킹·history 원자 이동·5개 상한·실패 복구·빈 history 디렉터리 미잔존 | 통과 |

### 12.2 골든 판정표와 부재 증명 신호

`fixtures/vulnerable-app/` 은 취약점을 고의로 심은 가짜 앱이고, `fixtures/golden.json` 은 사람이 작성한 정답 판정표다.

**부재 증명 항목은 픽스처에 대응 신호가 없으면 구조적으로 false pass 한다.** 스캐너가 아무것도 못 찾았을 때 "없음이 증명됐다"고 판정하는데, 애초에 심어놓은 게 없으면 그 판정이 옳은지 알 수 없기 때문이다. 그래서 1:1 로 못 박는다.

| 부재 증명 항목 | 픽스처에 심는 신호 | 기대 hit 규칙 |
|---|---|---|
| R-rrn | 주민번호 형식 문자열 · `jumin` 변수 | `rrn-data` · `rrn-field` |
| R-secrets | `AIza…` 키 · 주석 속 키 · `NEXT_PUBLIC_…SECRET` · 하드코딩 비밀번호 | `google-api-key` · `comment-secret` · `nextpublic-secret` · `hardcoded-password` |
| R-admin-data | 이름·학번·연락처 열이 있는 csv/xlsx | `admin-data-columns` |
| S-injection | `innerHTML` 에 변수 결합 · `eval` | `innerhtml-dynamic` · `eval-usage` |
| S-https | `http://` 리소스·`fetch` | `http-resource` |
| S-tracking | GA 태그(`gtag(` · `G-XXXXXXX`) | `analytics-tracking-script` |
| S-log-pii | `console.log(user)` | `console-sensitive` |

`[REQ-12.1]` 부재 증명 항목 **전수**에 대해 픽스처가 최소 1개의 hit 을 만들어야 한다. 대응 신호가 없는 부재 증명 항목이 하나라도 있으면 테스트를 실패시킨다.

`[REQ-12.2]` `golden.json` 은 `expect_scan`(`must_hit`·`must_not_hit`)과 `expect_items`(항목 id → 기대 판정), 그리고 `absence_proof_signals`(부재 증명 항목 id → 기대 hit 규칙 id 목록)를 담는다.

### 12.3 문서 린터

문서가 구현과 어긋나는 것을 사람의 주의력이 아니라 테스트로 막는다. `npm test` 에 포함한다.

| 린터 | 확인하는 것 |
|---|---|
| **REQ 커버리지** | ① 이 문서의 모든 REQ 가 구현 계획의 어떤 Task 에 할당됐다 ② 구현 계획이 참조한 REQ 가 이 문서에 실재한다 ③ 구현 계획의 전사 인용문이 이 문서의 원문과 **문자열 일치**한다 |
| **문서↔구현 동기화** | ④ §6 ↔ `items.json` — 항목 37개와 하위 점검 134개의 id·중요도·`methods`·`applicability`·`absence_proof`·`required_coverage`·`stacks`·`basis`·`why_risky`·`fix_hint` 를 **양방향** 대조 ⑤ §9·§9.1 ↔ `scan-rules.mjs` — 패턴 규칙 44개와 프로젝트 규칙 4개의 id·`item`·`subcheck`·`severity`·`stacks`·플래그를 양방향 대조 ⑥ §8.3 ↔ `report.contract.json` — 필드 경로·타입·허용값·렌더 위치를 양방향 대조 ⑦ §7.5 ↔ `session.json` — (항목 id, kind) 조합·질문 문구·답변 형식·갱신 대상을 양방향 대조. `methods` 에 `evidence`·`teacher` 가 있는 항목은 §7.5 에 행이 있어야 한다 |
| **규칙 정합성** | ⑧ `secretValue` 인데 마스킹되지 않는 규칙이 없다(REQ-9.9) ⑨ 모든 규칙의 `item`::`subcheck` 가 §6 에 실재한다 |
| **문서 위생** | ⑩ 두 문서에 드라이브 문자로 시작하는 절대경로나 다른 저장소 경로가 없다 ⑪ §5.1 단계표에서 "대기·승인"이 **있음**인 행의 "사람이 없을 때" 칸이 비어 있지 않다 ⑫ §9·§9.1 표의 규칙 id 48개가 전부 구현 계획 Task 2 에 정규식 또는 판정 함수와 함께 등장한다 |

`[REQ-12.3]` 위 12가지 확인은 `npm test` 에 포함된다. 문서와 구현이 어긋나면 테스트가 실패한다.

`[REQ-12.4]` ④⑤⑥⑦ 의 대조는 **양방향**이다. 문서에만 있는 id 와 구현에만 있는 id 둘 다 실패다.

### 12.4 계약 거부 테스트

`[REQ-12.5]` §8.3 계약(`report.contract.json`)에서 거부 테스트를 **자동 생성한다**. "렌더 위치"가 있는 모든 행에 대해 그 필드를 계약 위반 상태로 훼손한 보고서를 만들고, 검증기가 반드시 오류를 내는지 확인한다. 훼손 전략은 타입별로 정한다 — 배열↔객체 교체, 원소 필수 키 삭제, 허용값 밖의 값, 필드 자체 삭제.

`[REQ-12.6]` 계약에 행을 추가하면 거부 테스트가 자동으로 늘어난다. 검증기를 고치지 않으면 테스트가 실패한다.

### 12.5 현실성 확인과 출시 게이트

| 구분 | 대상 | 방법 | 기준 |
|---|---|---|---|
| **필수** | 픽스처 골든 대조 | 스킬을 픽스처에 실행하고 `golden.json` 과 대조 | **고위험(상) false negative 0 · false pass 0 · 잘못된 `na` 0**. 잘못된 `needs_human` 은 건수만 기록 · 근거 위치 정확 |
| **필수** | 비대화형 실행 | `claude -p` 로 픽스처에 1회 실행 | **사람의 응답 없이 보고서 4파일이 완성된다.** 중간에 멈추면 실패 |
| **필수** | 기존 빌드 산출물 | 픽스처에 `dist/` 를 두고 비대화형 실행 | `coverage.build` 가 `{status:"ran", reason:"prebuilt-artifact"}` 이고 안내 문구가 나온다 |
| 선택 | Codex | 픽스처로 Codex CLI 에서 `$edusafe` 실행 | 골든 판정표 대조 동일 기준. Claude Code 와 판정 수치가 같다 |
| 선택 | 실제 앱 ① | Next.js + Supabase 규모 앱 — 사람이 수동 골드 판정을 만들고 대조 | 상 항목 false pass 0, 오판 목록화 |
| 선택 | 실제 앱 ② | 공개 Firebase 앱 | 수동 골드 대조. 판단불가 감소량 관찰(목표치는 두지 않음), 오판 목록화 |

선택 항목을 수행할 수 없으면 그 사실과 이유를 실행 기록에 남긴다. 조용히 건너뛰지 않는다.

`[REQ-12.7]` 비대화형 실행은 선택 검증이 아니라 출시 게이트다. 스킬이 사람의 응답을 기다려 멈추면 산출물이 하나도 나오지 않기 때문이다.

## 13. 배포·버전

`[REQ-13.1]` `npm run build:zip` 은 `dist/edusafe-v<버전>.zip` · `dist/edusafe-v<버전>.sha256` · `dist/manifest.json` 을 만든다.

`[REQ-13.2]` `manifest.json` 은 정규화 규칙(경로 구분자 `/`, LF 줄바꿈, 경로 오름차순, symlink 제외)과 파일별 `{path, sha256}`, 그리고 그 목록에서 계산한 `skill_digest` 를 담는다. 신뢰 증거가 아니라 **구성 대조 자료**다(§11).

`[REQ-13.3]` 버전 정본은 `rules/version.json` 이다. README·보고서·zip 이름이 이를 참조한다.

안내 웹페이지(별도 작업)는 zip 다운로드 + 도구·OS별 설치 안내 + 현재 버전·ZIP SHA-256·`manifest.json`·공식 지문 + 신뢰 경계 문구를 싣는다.

## 14. 후속 과제

- 심사자 모드(무인·격리·질문 없음) — 트랙 2 담당과 인터페이스 합의 후
- 빌드 격리(환경변수 허용목록·네트워크 제한) — v0.1은 승인 + 위험 고지로 대응
- 트랙 2와 `rules/` 물리 공유
- 신규 항목 ID(S-password-storage·S-auth-hardening·S-api-overfetch·S-data-region·H-breach-ready·H-school-approval)를 트랙 2와 공유해 루브릭 정본 정합
- 생략한 원문 정독: 교육분야 가명·익명정보 가이드, 개인정보 처리 통합 안내서, KISA 개발보안·AI 보안 계열
- HTML 필터 기능(JS 없이 CSS만으로)
- Google Apps Script 등 비정식 스택 규칙
- 안내 웹페이지 제작

## 15. 참고 — 근거 서지

항목의 `basis` 는 아래 원문을 직접 대조해 적었다. 판(版)과 시행일이 자주 바뀌므로 서지를 정확히 남긴다.

| 문서 | 판·시행 | 이 설계에서 쓰인 곳 |
|---|---|---|
| 개인정보 보호법 · 시행령 | — | 제3·15·16·17·21·22의2·23·24의2·26·28의8·29·30·34·36조, 시행령 제17조의2·제31조① |
| 개인정보의 안전성 확보조치 기준 | **고시 제2026-9호**, 부칙 2026.7.1 발령한 날부터 시행(제15조5호·제18조는 2027-01-01) | 제3·4·5·6·7·8·9·10·12·13조 |
| 교육부 개인정보 보호지침 | **훈령 제476호** | 제5조②4(보호책임자 = 원장·교장) · 제13조①②(72시간 신고와 예외) · [별표1] 보유기간 책정 기준표 |
| 교육부 정보보안 기본지침 | **예규 제82호** | 제27조 · 제41조②1(국내 저장 요건) · 제52조④ · 제68조 |
| 교육부 「학습지원 소프트웨어 선정 기준 및 가이드라인」 | **2025. 12.** | 적용대상 트리거 ①② · [서식 1] 필수기준 |
| 「초·중등교육법」 제29조의2 | 2025-08-14 신설, **2026-03-01 시행** | H-school-approval |
| 아동·청소년 개인정보 보호 안내서 | **2024. 12.** | 1-1·1-2·1-3·1-4 · p.31~32 · 4-2(5일 이내) |
| 개인정보 처리방침 작성지침 | **2026. 4.** | 법정 기재사항 |
| 생성형 AI 개발·활용 개인정보 처리 안내서 | 2025 | p.27(개인용 vs 기업용 API 라이선스) |
| API 방식 개인정보 처리 시 유출 예방 안내 | **2026. 7.** | 권한 확인 · 응답 최소화 · 대규모 조회 제한 · 잔존 API |
| KISA 소프트웨어 개발보안 가이드 | — | 입력데이터 검증 및 표현 |
| KISA JavaScript 시큐어코딩 가이드 | — | 2절 1·2·3·5·6·9·12·13·14·16 · 에러처리 1 · 캡슐화 2 |
| 교육분야 인공지능 윤리원칙 | — | R-crisis(안전 보장) |

서지 주의: 팀 근거 문서 「에듀 세이프 심사 기준의 근거」에는 안전성 확보조치 기준을 "제2025-9호, 2025-10-31 시행", 아동·청소년 안내서를 "2022"로 적은 오기가 있다. 이 설계는 위 표의 판을 따른다.
